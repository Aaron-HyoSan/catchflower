# iOS 구현 현황 — 무엇이 되고 무엇이 안 되나

**기준일 2026-08-06 (5차 갱신)** · 코드 실측(`ios/CatchFlower` 42파일 5,595줄) · 단위 58 / UI 33 통과
(UI 33 중 2개는 네트워크·쿼터를 쓰는 실호출 테스트로 **기본 실행에서 제외**된다)

이 문서는 **안 된 것을 찾기 위한 문서**다. 된 것은 짧게, 안 된 것은 이유까지 적는다.
`진행.md`가 시간순 기록이고, 이 문서는 **지금 시점의 단면**이다. 갱신 시 둘 다 본다.

> **4차 대비 바뀐 것:** **A-1 실측 200장 완료 — Top-1 77%로 합격선(70%) 통과.**
> 실측이 학명 색인 버그를 잡았다(정답 0.606이 오답 0.003에게 지고 있었다).
> **시뮬레이터에서 PlantNet 실호출을 검증했다** — 실사진 픽스처 3장을 번들에 넣어
> `PlantNetRecognizer`가 앱 안에서 실제로 도는 것을 확인했다(전에는 실행 0회).
> **실기기의 남은 미검증 항목은 카메라 하드웨어뿐이다** — 판별은 검증됐다.
> ⚠️ **새 오너 대기 2건:** `identifyFailureFloor`와 난이도 임계값이
> 실측과 어긋난다(B-3-a · B-3-b). 숫자는 바꾸지 않았다.

---

## 0. 한 장 요약

| 구분 | 화면 | 상태 |
|---|---|---|
| 도감 | 04 · 05 · 06 · 22 | ✅ 동작 |
| 촬영~판별 | 07 · 08 · 09 · 10 · 11 · 12 · 13 | ✅ 동작 |
| 하루중복 안내 (B-5) | 번호 미부여 | ✅ 추가 (문구는 **오너 검토 대기**) |
| 희귀종 추가 사진 (B-3 가드 ①) | 번호 미부여 | ✅ 추가 (문구는 **오너 검토 대기**) |
| 권한 안내 | 03 | ✅ 동작 |
| 공통 UI (토스트·권한·다이얼로그) | 번호 없음 | ✅ 동작 |
| 로그인 | 01 · 02 | ❌ **없음** |
| 지도 | 14 · 15 · 16 | ❌ **없음** |
| 랭킹·친구 | 17 · 18 · 19 | ❌ **없음** |
| 마이·시즌 | 20 · 21 | ❌ **없음** |

**화면 24개 중 12개(+2) 구현 · 12개 미구현.** 미구현 12개 중
**10개가 서버(A-2 Supabase 미확정) 없이는 껍데기밖에 못 만든다.**
**즉 남은 미구현은 범위(1~6단계) 밖이거나 오너 대기다.**

---

## 1. 구조적 미구현 — 5개 전부 닫혔다

### 1-1. ✅ 저장 (완료)

`DiscoveryStore`(actor · JSON 파일 · `.atomic`) + `PhotoStore`(`Documents/Photos/`).
`AppSession.record`/`updateVisibility`/`delete`가 전부 `persist()`를 부른다.
**앱을 껐다 켜도 도감이 남는다** — UI 테스트가 `terminate()` 후 재실행으로 직접 확인한다.

- **`Caches/`를 쓰지 않는다.** iOS가 임의로 비운다 → 도감 사진이 소리 없이 사라진다.
- **SwiftData를 쓰지 않았다.** `Discovery`가 이미 `Codable`이고 공유계약 snake_case와 1:1이라
  `@Model`로 바꾸면 계약 필드명을 손으로 다시 맞춰야 하고 서버 업로드 시 변환이 한 번 더 든다.
  수천 건에 쿼리·마이그레이션이 필요하지 않다. **오프라인 동기화 큐를 만들 때 다시 판단한다.**
- 깨진 파일은 `.corrupt`로 치워 두고 빈 상태로 시작 — 조용히 `[]`를 주면 사용자는 도감이 사라진 걸로 본다.

### 1-2. ✅ 사진 저장 (완료)

`photo_url`에는 **파일명만** 넣는다 — 앱 컨테이너 경로는 재설치·복원 때 바뀐다.
`DiscoveryThumbnail`이 실제 사진을 그리고, 없으면 회색 자리를 둔다.
화면 10·11 사진 스트립과 화면 05 `내 발견 기록`이 하드코딩 아이콘에서 벗어났다.

⚠️ **시뮬레이터에서는 여전히 회색이다.** 픽스처가 빈 `Data()`라 저장할 사진이 없다
(빈 파일을 만들면 `exists`는 true인데 `image`는 nil이어서 판단이 꼬인다 → 아예 저장 안 함).
**실사진으로 확인한 적이 없다** = 4절.

### 1-3. ⚠️ 위치 (코드 완료 · 실측 미완)

`LocationProvider`(`@MainActor @Observable`) + `KakaoPlaceService`(actor).
판별과 **동시에** 돌린다 — 순차면 사용자가 위치를 기다린다.
실패하면 throw가 아니라 **nil**이다: 권한 문구가 `도감 등록은 위치 없이도 할 수 있어요`라고 약속했다.

동 코드와 **구 코드를 둘 다 저장한다** — B-6(10명 미달 시 구로 확대)은 런타임에 동에서 구를 못 만든다.

❌ **장소명이 아직 안 온다.** 카카오 키는 유효하지만 **콘솔에서 카카오맵/로컬 서비스가 꺼져 있다**
(`disabled OPEN_MAP_AND_LOCAL service`). 5절 참고. 켜지면 코드 수정 없이 동작한다.

### 1-4. ✅ 확정 정책 — B-5 · B-3 가드 ① 둘 다 흐름에 붙었다

| 정책 | 상태 |
|---|---|
| B-5 같은 종+같은 장소 하루 1회 | ✅ **`CaptureFlow`가 부른다.** 전용 화면 + UI 테스트 2개(막히는 경로 / 장소 옮기면 되는 경로) |
| B-3 어뷰징 가드 ① 희귀종 추가 사진 | ✅ `GamePolicy.needsExtraPhoto` + 전용 화면 + UI 테스트 4개 |

**둘 다 "상수는 있는데 부르는 곳이 0건"이던 자리다.** 규칙을 숫자로 적어두면
구현한 것처럼 보인다 — 정책 상수는 **호출처를 세야** 구현 여부를 알 수 있다.

가드 ①의 설계 판단:
- **1순위는 통과시킨다.** AI 1순위를 받아들인 건 어뷰징의 모양이 아니다.
- **점수를 조건에 넣지 않았다.** 희귀종은 표본이 적어 점수가 원래 낮다 —
  점수까지 보면 정직한 발견을 더 자주 막는다.
- 면제 표시를 **이탈 3곳에서 전부 지운다.** 남은 면제가 곧 우회 경로다.
  단 `다시 고르기`는 면제를 **주지 않는다.**

⚠️ **이 경로는 그냥 찍어서는 밟을 수 없다.** `귀함`은 200종 중 10종이고
개화기가 2~10월에 몰려 있다 → 픽스처 `찍기 (애매 · 2순위 희귀종)`로 밟는다.
11·12·1월에는 후보에 희귀종이 없어서 개화월 밖 희귀종으로 대체한다.

### 1-5. ✅ 공통 UI 컴포넌트 3종 (완료)

문구는 A 문서 3절 그대로다. **세 컴포넌트 모두 `String`이 아니라 enum을 받는다** —
호출부가 문자열을 넘기게 두면 문구가 코드 곳곳에서 창작된다.
컴파일러가 A 문서를 대신 지킨다. **새 문구는 A 문서에 먼저 추가하고 case를 늘린다.**

- **토스트 7종** (`Toast.swift`) — `나만 보기` / `공유하기` / `공유하지 않기`에 붙었다.
  마지막이 중요하다: 공유를 안 해도 **도감에는 이미 등록됐다.**
  색만으로 오류를 구분하지 않는다(아이콘도 바뀐다) · 20자 넘으면 표시 시간 2.6→4초 ·
  새 토스트가 앞 토스트의 타이머를 취소한다.
- **권한 시트 3종** (`PermissionSheet.swift`) — 카메라만 필수(이탈 불가).
  권한을 거부하면 **설명도 나갈 길도 없는 검은 화면**이었다.
  ⚠️ 위치·연락처 이탈 버튼 문구는 A 문서에 없어서 내가 뒀다 → **오너 검토 대상**.
- **확인 다이얼로그 3종** (`ConfirmDialog.swift`) — 이탈·삭제·공유취소.
  ⚠️ **`confirmationDialog`을 쓰면 안 된다.** 이 런타임에서 **취소 버튼이 렌더링되지 않아**
  나가는 길이 바깥 탭뿐이었다 = 제스처 전용 = 타깃 제약 위반. `alert`으로 확정.
  삭제도 스와이프가 아니라 **눌리는 버튼**이다.

**부수 수정:** 네트워크 오류가 화면 12(`어떤 꽃인지 알 수 없었어요`)로 가고 있었다.
사진 문제가 아닌데 "다시 찍어 주세요"는 거짓말이고, B-11 연속 실패 카운터까지 올랐다.
이제 토스트로 빠지고 카메라로 돌아온다.

---

## 2. 미구현 화면 12개 — 무엇이 막고 있나

### 2-1. 로그인·온보딩 01·02 (03은 완료)

| 화면 | 막는 것 |
|---|---|
| 01 로그인 | 카카오/애플 로그인. **A-2 백엔드 미확정** (키는 받았다) |
| 02 지역선택 | **카카오맵/로컬 서비스 활성화 대기** (키는 유효) |
| 03 권한안내 | ✅ 완료 |

지금도 `userID = UUID()`로 **매 실행마다 새 사용자**고 `regionName = "연남동"` 하드코딩이다.
⚠️ 저장이 붙었으니 이제 이게 실제 문제다 — **기록은 남는데 주인이 매번 바뀐다.**

**화면 03에서 연락처를 묻지 않는다.** `CNContactStore.requestAccess`가
시뮬레이터에서 **대화상자도 없이 반환하지 않았고**(권한을 하나씩 초기화해 갈라냈다),
CTA가 영구 비활성이 됐다. 쓰는 코드도 없다(친구 연결 = 화면 19) →
**화면 19를 만들 때 그 자리에서 요청한다.** 전체 요청에 60초 상한을 둔 것도
같은 이유다 — 시스템 호출이 안 돌아오면 사용자가 죽은 버튼 앞에 갇힌다.

### 2-2. 지도 14·15·16 — 카카오맵 서비스 활성화 대기

`MapKit` 참조 0건. A-3는 카카오맵 확정. 키는 있고 **콘솔 토글이 꺼져 있다.**
탭을 누르면 `NotBuiltYetView(지도, 14)` — **빈 화면과 버그를 구분하려고** 일부러 넣었다.

### 2-3. 랭킹·친구 17·18·19 — 서버 없이는 의미가 없다

랭킹은 남과 비교하는 기능이라 로컬로 못 만든다. B-6은 서버 집계 규칙이고 친구는 연락처 해시(C-3)가 필요하다.
**더미로 만들 수는 있다.** 다만 가짜 순위를 그리면 검수자가 랭킹이 동작한다고 착각한다.

### 2-4. 마이·시즌 20·21

내 통계는 로컬 가능, **배지·시즌 집계는 서버**.

---

## 3. 되는 것 중에도 '진짜'가 아닌 것

| 항목 | 지금 | 진짜가 되려면 |
|---|---|---|
| 꽃 인식 | ✅ **실측 완료 · Top-1 77%** (라벨 200장) · 시뮬레이터에서 실호출도 검증됨 | 남은 건 **실기기 카메라로 찍은 사진**(압축 결과가 다를 수 있다) |
| 판별 임계값 | ⚠️ **실측과 어긋난다.** `floor=0.30`이 정답 43%를 버린다 | **오너 답변 B-3-a·B-3-b** — 숫자는 바꾸지 않았다 |
| 꽃 그림 | `FlowerSymbol` — 과(科)로 꽃잎 수를 정하는 도형 | 일러스트 200종(D-1). 오면 이 파일만 지운다 |
| 꽃 이야기 | CSV `habitat`+`bloomLabel` **조립** | D-2 초안. **문구를 창작하지 않으려고** 조립만 했다 |
| 장소명 | `서울숲`(픽스처 예시값) | **카카오맵 서비스 활성화** |
| 사용자 | 실행마다 새 `UUID` | 로그인(01) + A-2 |
| 화면 09 확신도 | **표시 안 함** | 의도된 것. `55%`를 보여주면 맞는 답을 사용자가 거절한다 |
| 화면 10 순위 변동 | **빈 줄** | 서버 랭킹(B-6) |
| B-5 안내 문구 | A 문서 3절에 추가 · **오너 검토 대기** | 오너 확인 |
| 권한 시트 이탈 문구 | 위치·연락처는 A 문서에 없어 내가 뒀다 · **검토 대기** | 오너 확인 |
| B-3 가드 ① 문구 | A 문서 3절에 추가 · **오너 검토 대기** | 오너 확인. **가장 조심해야 하는 문구다** — 귀한 꽃을 만난 정직한 다수를 의심하는 말투가 되면 안 된다 |
| 화면 03 연락처 권한 | **묻지 않는다** (2-1 참고) | 화면 19 구현 시 그 자리에서 |

---

## 4. 실기기 — 남은 건 카메라 하드웨어뿐이다

**4차 때는 이 절이 "최대 리스크"였다. 범위가 줄었다.**

| 항목 | 상태 |
|---|---|
| **꽃 인식 정확도** | ✅ **검증됨** — 라벨 200장 Top-1 77% (`ios/Tools/plantnet_measure.py`) |
| **`PlantNetRecognizer`가 앱에서 도는가** | ✅ **검증됨** — 시뮬레이터 실호출 3경로 통과 |
| 서명 | ✅ **된다** — 무료 Personal Team, arm64 서명 빌드 성공 |
| `AVCaptureSession` 실동작 (권한·플래시·전환) | ❌ 실기기 필요 |
| `UIImage.downscaledJPEG` 실사진 결과·소요 시간 | ❌ 실기기 필요 |
| `LocationProvider` 권한 대화상자 | ❌ 실기기 필요 |
| 도감 썸네일이 실제 사진으로 보이는지 | ❌ 실기기 필요 |

### 어떻게 실기기 없이 판별을 검증했나

**전에 막혀 있던 이유는 카메라가 아니라 픽스처가 빈 `Data()`였던 것이다.**
보낼 사진이 없으니 시뮬레이터는 항상 Mock으로 우회했고, 그래서
`PlantNetRecognizer`는 **실행 횟수가 0이었다.**

실사진 3장(268KB)을 번들에 넣고 `CaptureFlow`가 **바이트가 있으면 실엔진**으로
보내게 했다. 사진 출처·라이선스는 `CatchFlower/Resources/RealFixtures/README.md`.
⚠️ **출시 전에 `project.yml`에서 `RealFixtures` 등록을 뺀다** (TF-flowers는 개발용).

| 버튼 | 실측 점수 | 도달 화면 |
|---|---|---|
| 실호출 · 해바라기 (0.99) | 0.994 | 확정 09 ✅ |
| 실호출 · 해바라기 (0.57) | 0.570 | 애매(후보 3개) |
| 실호출 · 민들레 (0.11) | 0.112 | 판별 실패 12 ✅ |

**실호출 테스트는 기본 실행에서 빠져 있다** (네트워크·일일 쿼터 500).
켜는 방법은 `UITests/RealRecognitionUITests.swift` 상단 주석에 있다
(환경변수는 XCUITest 러너에 닿지 않는다 — 시뮬레이터 공유 tmp의 파일로 켠다).

### ⚠️ 실측이 잡은 것 — 여기가 이번 갱신의 핵심이다

**학명 색인이 정답을 버리고 있었다.** genus fallback이 속 대표를 도감번호
최솟값으로 고정해서, **그 대표의 개화기가 끝나는 달부터 그 속 전체가 판별 불가**였다.
8월 장미: `Rosa chinensis 0.606`(정답) → 찔레꽃(5~6월)로 번역 → 필터 탈락 →
`Begonia grandis 0.003`이 1순위. 200종 중 94종이 다종 속이고 30속은 개화월이 다르다.

→ 색인이 **후보 집합을 받아** 속 안에서 제철 종을 고르게 고쳤다.
→ **앱을 켜서는 못 본다** — "AI가 틀렸네"로 보인다. **달을 바꿔 재야 드러난다.**
   회귀 테스트를 그 모양으로 고정했다.

**유료 API 실측 보고 전 금지 규칙**과 무관하다 — PlantNet은 무료 티어이고,
실측이 곧 그 보고다. **원본 응답 200개를 캐시했으므로 재채점에 쿼터를 쓰지 않는다.**

---

## 5. 오너 대기

| 항목 | 상태 | 막는 것 |
|---|---|---|
| **카카오 콘솔: 카카오맵/로컬 활성화** | ⛔ **지금 막힘** | 장소명 · 지도 14~16 · 지역선택 02 |
| **Supabase 프로젝트 URL** | ⛔ 필요 | `sb_publishable_` 키는 불투명 문자열이라 **URL 역산 불가** |
| **A-2 백엔드 확정** | 미확정 | 랭킹·친구·시즌 · 계정 |
| **B-3-a `identifyFailureFloor`** | ⛔ **실측 근거 올림** | **정답 43%를 버린다.** 0.30 → 0.05 권고 (`오너_결정사항.md` B-3 실측 절) |
| **B-3-b 난이도 임계값 60/70/85** | ⛔ **실측 근거 올림** | 0.60 넘긴 사진이 28%뿐 — **10장 중 7장이 애매 화면으로 간다** |
| **B-4 민들레류 통합** | ⛔ **실측 근거 올림** | PlantNet이 `Taraxacum sect.`만 준다(종 미구분) — 안 합치면 **채울 수 없는 칸**이 생긴다 |
| **B-5 안내 문구 검토** | 신규 | 내가 A 문서에 추가한 초안 |
| **권한 시트 이탈 문구 검토** | 신규 | 위치·연락처 이탈 버튼 (A 문서에 없던 자리) |
| **B-3 가드 ① 문구 검토** | 신규 | `귀한 꽃이네요!` — 검증이 아니라 대접으로 읽히게 썼다 |
| ~~Apple 개발자 계정~~ | ✅ 로그인됨 (무료 Personal Team) | — · **서명 빌드 성공** |
| **기기 케이블 1회** | ⛔ 필요 | **카메라 하드웨어만** — 판별은 검증됐다(4절) |
| ~~PlantNet 키~~ | ✅ 받음 | — |
| ~~카카오 REST 키~~ | ✅ 받음 (서비스는 꺼짐) | — |

---

## 6. 다음 순번

**오너가 정한 범위(1~6단계)는 닫혔다.** 여기서부터는 전부 범위 밖이거나 오너 대기다.

| 순 | 일 | 막는 것 |
|---|---|---|
| 1 | **오너 답변 B-3-a · B-3-b** | ⛔ 오너. **근거는 다 올렸다** — 숫자 두 개만 정하면 코드는 한 줄씩이다 |
| 2 | **daisy 42.5% 원인 분리** | 없음. 사진을 직접 봐야 한다 (아래) |
| 3 | **실기기 카메라 1회** (4절 하단 4항목) | ⛔ 케이블. **서명·판별은 끝났다** |
| 4 | 랭킹·마이 더미 (17·20) | 없음. **가짜 숫자는 "준비 중"으로 표시** |
| — | 장소명·지도 14~16 | **카카오 콘솔 토글** |
| — | 로그인 01·02 | **A-2 + Supabase URL** |
| — | 친구 19 (+ 연락처 권한) | **A-2** · C-3 연락처 해시 |

**4차 때 이 자리에 "시뮬레이터로 할 수 있는 일이 남지 않았다"고 적었는데, 틀렸다.**
막고 있던 건 실기기가 아니라 **빈 픽스처**였다(4절). 판별 정확도·색인·개화월 필터·
화면 분기가 전부 시뮬레이터에서 검증됐고, 그 과정에서 실제 버그가 하나 나왔다.
**"실기기 없이는 못 한다"는 판단을 다시 의심해 볼 것** — 이번엔 그게 200장 실측과
색인 버그를 한 달 늦출 수 있었다.

⚠️ **2번은 "아직 답을 모르는 것"이라 적어 둔다.** 클래스별 Top-1에서
**daisy가 42.5%로 가장 낮다.** 원인이 두 가지인데 아직 못 갈랐다:
① TF-flowers의 `daisy` 라벨이 느슨하다(`Leucanthemum`·`Argyranthemum`이 섞여 있다) —
   그러면 우리 문제가 아니다
② 우리가 국화과에 실제로 약하다 — 그러면 도감 5종을 손봐야 한다
**숫자만 보고 "국화과가 약하다"고 말하지 않는다.** 사진 40장을 눈으로 봐야 갈린다.
