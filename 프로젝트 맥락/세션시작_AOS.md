# Android 세션 시작 문서

이 터미널은 **Android 전담**이다. 새 세션을 열면 이 파일부터 읽는다.

---

## 0. 30초 요약

**캐치플라워** — 꽃을 찍으면 AI가 종을 판별해 도감에 모으고, 지도에 남기고,
동네·친구끼리 시즌 랭킹으로 경쟁하는 앱. 화면 22개 **+ 번호 밖 3개**
(00 로딩 · 20-1 프로필 수정 · 20-2 설정 — **와이어프레임에 짝이 없다**) ·
도감 **2,057종**(그리는 **칸은 2,044개** — B-4로 13종을 한 칸에 접었다 · 계약 1-5·1-6).
🔴 **"200종"은 낡은 숫자다** — (53)에서 확장했다. 200종 CSV는 원본 씨앗으로만 남아 있다.

**기획은 끝났다. 코드는 main 23,733줄 + 테스트 18,395줄(JVM 15,896 · 계측 2,499)이고
화면 22개 중 21개가 돈다**(2026-08-17 기준 · **27차**).
⚠️ **커밋 수·ahead 수를 여기 적지 않는다** — 적는 순간 그 커밋이 값을 낡게 만든다(두 번 겪었다).
`git rev-list --count HEAD` · `git rev-list --count origin/main..HEAD`로 그때 센다.
**iOS는 다른 터미널에서 동시에 진행 중이다.**

⚠️ **`프로젝트 맥락/구현현황_AOS.md`가 지금 시점의 단면이다.** 이 파일은 환경·규칙이고,
"무엇이 되고 무엇이 안 되나"는 그 문서를 본다. **되는 것 중에도 진짜가 아닌 것이 있다**(3절).

🎉 **오너가 정한 예선 범위(1~7단계)에 미구현 화면이 없고, 죽은 버튼이 0개다**((74)(75)).
판별은 **실기기에서 PlantNet 실호출로 돈다**(누적 **23건**/500건·일 · (43)(49)).
지도 타일·핀·사용자 사진까지 실기 확인됐다. 남은 미구현은 **화면 01(로그인)** 하나이고,
그 자리는 **로그인 시트**가 대신한다((76) · 아래).

🔒 **비로그인 게이트가 붙었다((76) · 2026-08-14 오너 확정 · (81)에서 두 개 늘었다).**
열람(도감·지도·랭킹·남의 기록)은 제한 없음 / 촬영→판별→등록→공유는 **판별 성공 2회까지** /
댓글·좋아요·신고 **+ 친구 요청 · 닉네임 변경**은 **0회, 로그인 필요**.
**서버·RLS·마이그레이션 변경 0줄**이다 — 익명 세션을 그대로 두고 액션만 막는다.
🔴 **익명 세션을 없애면 읽기가 죽는다**(RLS가 `auth.uid()`를 탄다).

🔴 **(81) 2026-08-16: 오너가 폰에서 구멍 두 개를 찾았다** —
`마이프로필이나 설정을 바꿀때면 로그인이 없어도되네?` 닉네임 변경·친구 요청이
**`GatedAction`에 아예 없어서** 게이트를 지나지 않았다. **exhaustive `when`도, 값 목록
검사도, grep도 전부 초록이었다**(없는 것은 못 찾는다) 그리고 **증상도 없다**(막혀야 할 것이
그냥 된다). → 8개로 늘리고 **`선언된_행동은_전부_부르는_곳이_있다`**를 짝으로 넣었다.
⏸ **지역 선택(화면 02)은 일부러 안 막는다** — 온보딩이 로그인 전에 `dong_code`를 쓴다.
🔴 **서버에 쓰는 새 자리를 만들 때마다 `GatedAction` 표를 본다**(코드로 못 막는 방향이다).

✅ **도감이 좁아서 후보가 1개만 남던 구멍은 닫혔다** — 2,057종 확장((53) · 후보 집합
70 → 1,590종 · Top-3 83.0%)에 이어 **B-4 수집 그룹 8개**를 붙였다((78) · 계약 **1-6**).
사진으로 못 가르는 13종을 대표종 한 칸으로 접어서, **달에 따라 등록 칸이 갈리던 사진
28장 → 0장**이 됐다. ⚠️ **접는 순서가 설계다**(`후보(종) → 매칭(종) → 접기(그룹)`) —
뒤에서만 접으면 **후보 3칸이 2칸으로 준다.** ✅ **남은 오너 결정도 2026-08-16에 닫혔다**
(남은 3항목은 **"안 고쳐도 된다"** · (80)) — 즉 아래 목록에서 B-4는 **끝난 칸**이다.

🆕 **화면 00 로딩 화면이 붙었다((80) · 오너 지시).** 흰 배경·배지·`CatchFlower`·
푸터 `SUJI & SAN corp`. 🔴 **가짜 로딩이 아니다** — 도감 2,057종 파싱을 여기서 IO로
데운다(148ms 실측). 지우면 그 시간이 **첫 화면 메인 스레드로 돌아간다.**
⚠️ **배지 크기는 세 곳이 한 값으로 묶여 있다**(생성 스크립트 `BADGE_DP` ·
`values-v31/themes.xml` · `drawable/splash_background.xml` · 화면 00) —
108dp/160dp로 어긋나 있던 것을 고쳤고, **어긋나도 빌드·테스트·스크린샷은 전부 통과한다.**

🚀 **2026-08-17: 출시 준비를 전부 훑었다((82)) — 원본은 `프로젝트 맥락/출시준비_AOS.md`다.**
막는 것은 ~~**둘**~~ → **둘이 맞는데 구성이 바뀌었다**(①이 닫히고 ③이 생겼다 · 증상이 셋 다
다르다): ~~① **법적 문서 빈 칸 6종** → `bundleRelease`가 **멈춘다**~~
🔵 **①은 2026-09-08에 완전히 닫혔다((95))** — 오너가 6칸을 줘서 `docs/legal/` 5장이 처음
구워지고 `bundleRelease`가 escape hatch 없이 통과했고, ~~커밋·푸시 + 두 URL 열림 확인~~도
**같은 날 끝났다**(`06afc1f`·`455b0d5` · Pages 빌드 `error: null` · 내려온 바이트 sha256 일치).
🔴 **`푸시했다`도 `HTTP 200`도 확인이 아니다** — 캐시의 낡은 판도 200을 주고, `custom_404:
false`면 없는 파일까지 뿌리 페이지로 200이 온다.
② **카카오 릴리스 키해시** → 빌드·업로드는 되고 **심사자가 지도를 눌렀을 때** 죽는다.
③ **테스터 15~16명 × 연속 14일**(F-1이 개인 계정으로 뒤집혀 생겼다 · (90)) → 빌드도
업로드도 되고 **`Apply for production` 버튼이 안 열린다.** 🔴 **①②는 업로드를, ③은
프로덕션을 막는다.** 그리고 🔴 **공개해 둔 해커톤 APK가 ③을 직접 깎는다**(같은 패키지·
versionCode·**다른 서명** → 그것이 깔린 폰은 스토어 설치가 실패하고 **화면에 이유가 안
나온다**) — 고침은 코드가 아니라 안내문이고 문장은 `출시준비_AOS.md` **4-9**에 있다.
그 밖에 코드·산출물 쪽에 막는 것은 없다(다운로드 **58.4~64.7MB**/200MB · 16KB 정렬 통과 ·
targetSdk 36). 🔴 **업로드 뒤에 하나 더** 있다 — Play 앱 서명 SHA-1도 카카오에 등록해야
**스토어로 받은 사람**의 지도가 산다.
🔴 그 훑기에서 **초록 상태의 결함 3개**가 나왔다(2-27~2-30): 랭킹이 앱 시작 응답을 계속
그렸고, **약관 화면 3종이 기기에서만 100% 죽었고**(ICU 정규식 · JVM에서는 원리상 못 잡는다),
**내 산출물 검사가 다운로드 크기를 한 번도 안 쟀다.**
🔴 **(83)에서 네 번째가 나왔다(2-31) — 권한이 기기 필터를 얹고 있었다.** CAMERA·위치
권한이 `uses-implied-feature`로 **뒷면 카메라·위치 하드웨어를 필수**로 만들어, 매니페스트
주석이 말하는 것과 Play가 거는 조건이 달랐다. **앱 안에는 증상이 0이고**(손해는 "그 기기의
스토어에서 안 보인다"뿐) 내 폰으로는 원리상 못 본다 → `required="false"` 5줄로 고쳤고,
검사도 badging 기반으로 바꿨다(예전 판은 `android.permission.*`만 봐서 **광고 ID 권한을
못 봤다**).

⏸ **지금 답을 기다리는 것은 `Play 계정 유형(F-1)` 하나다.**
⏸ **오너가 콘솔에서 돌릴 것 3개는 7절·8절에 있다** — `05` 2절 · `0010` 2·3절 ·
**Redirect URLs 확인**(오너는 "수행완료"라고 했는데 **완주 증거가 아직 없다**).

📦 **저장소가 공개돼 있고 제출물이 올라가 있다 — 9절을 먼저 본다.**
🔴 **`main`이 `origin/main`보다 앞서 있다 — 아직 push 안 했다**(즉 **이 맥 밖에는 없다** ·
개수는 `git rev-list --count origin/main..HEAD`로 센다).
🎬 **제출 요건 5개가 전부 닫혔다((49)(50)).** 팀원 롤 기술서까지 채웠다
(김효산 기획·개발 / 정수지 기획·디자인 · 비중 60/40 · PDF 7페이지).
플레이 영상도 찍었다 —
`제출물/영상/catchflower_play_51s.mp4`(51초). **에뮬레이터 `virtualscene` 카메라로
실물 꽃 없이, 앱을 안 고치고, PlantNet 실호출 1건으로 찍었다.**
🔵 **유튜브 업로드는 오너가 접었다 — 다시 꺼내지 않는다**(9-1절).

---

## 1. 반드시 먼저 읽을 것 (순서대로)

| 순서 | 파일 | 왜 |
|---|---|---|
| 1 | **`프로젝트 맥락/구현현황_AOS.md`** | **지금 무엇이 되고 무엇이 안 되나 · 다음 순번. 여기부터 읽는다** |
| 2 | **`프로젝트 맥락/공유계약_iOS_AOS.md`** | **필드명·enum·정책 숫자·Mock 동작. 혼자 바꾸면 안 되는 것들** |
| 3 | **이 파일 8절** + **`프로젝트 맥락/출시준비_AOS.md`** | ⏸ **지금 이어서 할 일이 거기 있다** — 🚀 **출시는 그 파일이 원본**(막는 것 2개 · 순서 · 오너 몫) · Redirect URLs **완주 확인**(오너는 했다고 한다) · 게이트 실기기 확인 |
| 4 | `프로젝트 맥락/진행.md` **맨 끝부터 거꾸로** | 최근 결정이 뒤에 있다. **(80)(81)이 지금 맥락**이고((80)에 오너 답 6건이 한 번에 닫힌 표 · (81)에 게이트 구멍 두 개가 있다) (19)는 iOS인데 AOS도 물린다 |
| 5 | `디자이너_업무/A_문구·버튼_스펙.md` | **화면 문구가 전부 여기 있다. 새로 쓰지 않는다** |
| 6 | `프로젝트 맥락/오너_결정사항.md` | 확정/미확정 구분 (**36항목** · F-1이 새로 생겼다) |
| 7 | (필요시) `와이어프레임/index.html` | 화면 22개를 브라우저로 본다 |

**시간 없으면 1·2번만 읽고 시작해도 된다.**

---

## 2. ⚠️ 환경 — 버전 조합이 정해져 있다. 벗어나면 빌드가 안 된다

설치·검증 완료 (2026-08-04). **함정을 두 번 밟고 나온 조합이라 임의로 올리지 않는다.**

| | 값 |
|---|---|
| JDK | **OpenJDK 21.0.12** · `/opt/homebrew/opt/openjdk@21` |
| Gradle | **9.6.1** |
| **AGP** | **9.3.1** ← **8.x는 빌드 불가** |
| **Kotlin** | **AGP 내장** ← **`org.jetbrains.kotlin.android` 선언 금지** |
| compileSdk / targetSdk | **36** |
| minSdk | **26** (Android 8.0) |
| SDK | `~/Library/Android/sdk` (4.1GB) |
| Build-tools / platform-tools | 36.1.0 / 37.0.1 (adb 1.0.41) |
| 에뮬레이터 | 37.1.11 · **AVD `CatchFlower_Pixel8`** (arm64 · 부팅 40초) |

환경변수는 `~/.zshrc`에 영구화됨 (`JAVA_HOME` · `ANDROID_HOME` · `ANDROID_SDK_ROOT` · `PATH`).

### 두 함정의 정확한 증상

**1. Gradle 9.6.1 + AGP 8.x →**
```
Plugin 'com.android.internal.application' relies on
'org.gradle.api.problems.internal.InternalProblems',
a Gradle internal API that was removed in Gradle 9.6.0.
```
Gradle 9.6.0이 AGP 8.x가 쓰던 내부 API를 제거했다. → **AGP 9.3.1을 쓴다.**

**2. AGP 9 + kotlin 플러그인 →** `Cannot add extension with name 'kotlin'`
AGP 9에 Kotlin 컴파일러가 내장돼 있다. → **플러그인 선언을 뺀다.**
`kotlinOptions`·`compileOptions` 블록도 필요 없다.
컴파일 산출물이 `intermediates/built_in_kotlinc/`에 나오는 걸로 확인했다.

**동작 확인된 최소 build.gradle.kts (app):**
```kotlin
plugins { id("com.android.application") }
android {
    namespace = "..."
    compileSdk = 36
    defaultConfig { minSdk = 26; targetSdk = 36; versionCode = 1; versionName = "1.0" }
}
```
루트에서는 `id("com.android.application") version "9.3.1" apply false`.

### 에뮬레이터 실행

```bash
emulator -avd CatchFlower_Pixel8 -no-window -no-audio &   # 헤드리스, 40초
adb wait-for-device shell getprop sys.boot_completed        # 1이면 준비 완료
```
**arm64 네이티브 이미지라 빠르다. x86 이미지를 추가하지 않는다.**

### 이 맥의 제약

**`brew install --cask`가 안 된다** (sudo 비밀번호 입력이 없는 환경).
Android Studio·temurin cask 설치 실패한다. **formula와 zip 직접 전개로만 설치한다.**
지금 조합은 그 방식으로 깔아 검증했으니 **추가 설치가 필요하면 같은 방식을 쓴다.**

---

## 3. 할 일 — **1~7단계는 끝났다**

| 순서 | 일 | 상태 |
|---|---|---|
| 1 | 프로젝트 골격 | ✅ (13) |
| 2 | 데이터 모델 + `GamePolicy` object | ✅ (14) — 단 **호출처 0건 상수가 8개**다 (구현현황 4절) |
| 3 | 도감 적재 **2,057종**(칸 2,044) | ✅ — `꽃도감/flowers.json`을 **빌드마다 원본에서 복사**한다 |
| 4 | 도감 홈·상세·필터 (04·05·06·22) | ✅ (15) |
| 5 | Mock FlowerRecognizer | ✅ |
| 6 | 카메라~판별 (07~12) · CameraX | ✅ (17)(22) — **PlantNet 실호출로 돈다**(키 없으면 Mock 폴백) |
| 7 | 랭킹·친구·마이 (17~21) | ✅ (18)(31) — 더미 걷어냈다. 🔵 **`FriendsScreen`도 2026-08-13에 닫혔다**(`DummyRanking.kt` 삭제) — 이 칸이 오래 "남은 더미 하나"로 적혀 있었다. 남은 건 **연락처 매칭**(일부러 안 읽는다)과 **화면 21 시즌 스냅샷**(서버에 데이터 없음) |
| 8 | 온보딩·활동지역 (02·03) | ✅ (32) — **로그인 01은 예선 범위 밖**(익명 로그인으로 돈다) |
| 9 | 저장 계층 + 서버 업로드 + 지도 13·14 | ✅ (30)(35)(36)(37) — **실기 실측** |

**다음에 할 것은 `구현현황_AOS.md` 9절이 원본이다.** 지금 이어서 할 일은 8절에 있다.

⚠️ **`PlantNetRecognizer`는 이미 있다** (`recognizer/PlantNetRecognizer.kt`).
iOS가 실측 200장으로 잡은 버그(학명 색인 genus fallback — **정답이 오답에게 졌다**)는
**처음부터 피했다** — `ScientificNameIndex.flowerId(name, preferring = 후보집합)`이다.
🔴 **`preferring`을 안 넘기면 속 대표가 도감번호 최솟값으로 정해져 8월 장미가 찔레꽃(5~6월)로
번역되고 탈락한다.** 그 케이스는 `PlantNetRecognizerTest`가 고정하고 있다. `진행.md` (19) 3절.

---

## 4. ✅ Android 고유 숙제 — 끝났다 (절감 47%)

`AI인식_API_비용검토.md` 4절의 **절감 48%**가 "iOS Vision으로 꽃 여부를 공짜 판별"을
전제로 계산됐는데 **Android에는 Vision이 없었다.**

**결론: ML Kit Image Labeling으로 대체 가능. 실측 절감 47%** (가정 48%).
사진 1,000장 실측 · 번들 모델 · 과금 없음 · minSdk 26 지원. 수치는 `진행.md` (15).

⚠️ **`preFilter`를 `AlwaysPassPreFilter`로 바꾼 채 두지 않는다.**
화면 확인용으로 임시 교체한 적이 있고 원복했다 —
**남으면 모든 사진이 유료 API로 간다.** 차단율 92.4%가 곧 비용 설계다.
같은 이유로 `val month = 4` 고정도 원복했다 (**남으면 개화월 하드 필터가 죽는다**).
📌 지금은 `QaPreFilterSwitchTest`의 소스 검사가 이 둘을 막는다 — 4-1 참조.

### 4-1. QA용 1차 필터 우회 — 스위치는 **파일**이다 ((45))

화면(모니터·폰) 속 꽃 사진을 찍으면 1차 필터가 **옳게** 막는다(서브픽셀 모아레 →
`top=Pattern`·`Textile`). 그 판단은 맞지만 **뒤쪽 흐름(등록→도감→지도)을 손으로 QA할 수 없다.**

```bash
adb shell touch /data/local/tmp/cf_qa_allow_any_photo   # 켜기
adb shell rm    /data/local/tmp/cf_qa_allow_any_photo   # 끄기 (QA 끝나면 바로)
```
앱 재시작 불필요(촬영마다 파일을 본다) · **release 빌드에서는 파일이 있어도 안 열린다.**

| 하지 말 것 | 이유 |
|---|---|
| **UI 버튼으로 만들기** | 개발용 버튼을 사용자 화면에 두 번 남겼다((35)(38)). **화면에 없으면 남을 수도 없다** |
| **`BuildConfig.DEBUG`만으로 열기** | 디버그 빌드 **전체가 항상 우회**가 되어 1차 필터를 두 번 다시 검증할 수 없다 |
| **`enabled` 값 캐시** | `rm`으로 껐는데 재시작까지 열려 있다 — **끈 줄 알고 QA를 계속한다** |
| **`AlwaysPassPreFilter`로 교체** | 실제 필터가 0줄 돌아 **무엇을 우회했는지 로그에 안 남는다.** 감싸고 결과만 덮어 `QA우회(원래=Pattern)`을 남긴다 |
| **래퍼 안에서 `android.util.Log`** | JVM 테스트가 `not mocked`로 죽는다(실제로 3개 빨개졌다). 우회 표시는 `topLabel`에 싣는다 |

🔴 **켠 채로 잊으면 아무 사진이나 유료 API로 가는데 화면에는 증상이 없다.**
그래서 앱이 뜰 때 `🔴 QA 우회가 켜져 있다`를 경고로 찍는다.

---

## 5. 하지 말 것

| | 이유 |
|---|---|
| **AGP를 8.x로 내리거나 kotlin 플러그인 추가** | **빌드가 죽는다** (2절) |
| **공유계약 문서의 필드명·enum을 혼자 바꾸기** | iOS와 갈라진다 |
| **`꽃도감/꽃목록_200종.csv` 직접 수정** | **생성물이다.** 원본은 `꽃도감/_tools/flowers.py` |
| **와이어프레임 SVG 직접 수정** | 생성물. 원본은 `와이어프레임/_tools/screens_*.py` |
| **기획서 docx에 `patch_docx.py` 재적용** | **멱등이 아니다.** 원본백업에서 복원 후 적용 |
| **문구를 새로 창작** | A 문서에 이미 있다. 없으면 A에 추가하고 기록 |
| **개화기 `"3~4월"` 문자열을 클라이언트에서 파싱** | 서버/적재에서 한 번만 한다 (공유계약 1-2) |
| **DB 스키마를 만들기** | 🔵 **iOS가 이미 만들었다** — 진행.md (23) "스키마 확정" · `supabase/migrations/` 3개 · 읽는 법은 `supabase/README.md`. 고칠 게 있으면 `0004_*.sql`을 추가하고 진행.md에 남긴다 (기존 파일을 고치면 **이미 적용한 DB와 어긋난다**) |
| **랭킹을 클라이언트에서 세기** | B-6("동 10명 미만이면 구로 확장") 때문에 양쪽이 각자 세면 **다른 랭킹이 나온다.** `region_ranking()`·`friend_ranking()`을 부른다 — 📌 **`RankingRules` 16개 테스트가 이 자리에 있다**(정렬 규칙 자체는 같으니 버리지 않되, 화면 값은 서버 결과여야 한다) |
| **유료 API 호출** | 실측 보고 전 금지 (오너 규칙). PlantNet 무료 500/일 · **누적 23건**((49) 영상 녹화로 +7) |
| **`MIN_CONFIDENCE_FOR_ANY_CANDIDATE`(0.05) 건드리기** | **오너가 승인한 계약값**이다((44) B-3-a). iOS `identifyFailureFloor`와 짝이다. 낮은 점수를 보고 "높다"고 판단하지 않는다 — [[plantnet-score-is-not-confidence]] |
| **`CaptureViewModel`·`PlantNetRecognizer`의 진단 로그 4줄 지우기** | `1차필터:` → `판별 호출 직전:` → `판별 응답:` → `화면 12:`. **이게 없으면 화면 12의 원인 3가지와 "과금됐는가"를 구분할 수 없다**((39)(46)). 로그는 **조용해져도 증상이 없어서** 테스트로 고정해 뒀다 |
| **인식기 클래스 안에서 `android.util.Log` 부르기** | JVM 테스트가 `Method i in android.util.Log not mocked`로 죽는다. `log: (String) -> Unit = {}`을 주입받고 **호출처가 넘긴다** |
| **`unitTests.isReturnDefaultValues = true` 추가** | 거부된 항목이다 — **모든 안드로이드 API가 조용히 0/null**이 되어 파싱 결함이 초록으로 통과한다. `testImplementation("org.json:json")`도 빼지 않는다 |
| **`connectedDebugAndroidTest`를 돌리고 그냥 두기** | 🔴 **끝나면서 앱을 지운다.** 오너 폰에서 앱이 사라졌다 — `adb install -r`로 다시 깔고 `pm list packages`로 확인한다 |
| **`/data/local/tmp/cf_run_network_tests`를 남겨 두기** | CI가 유료 쿼터를 태운다. 쓰고 나면 지운다 |

---

## 6. 한 스텝 끝나면

**`프로젝트 맥락/진행.md`에 덧붙인다** (오너의 상시 규칙).
**세션 제목에 `[AOS]`를 붙인다** — iOS 세션도 같은 파일에 쓴다.
**`구현현황_AOS.md`도 같이 갱신한다** — 진행.md는 시간순, 그쪽은 현재 단면이다.

```
## (20) 2026-08-XX [AOS] · 무엇을 했나
```

⚠️ **화면을 만들었으면 에뮬레이터로 본다.** `BUILD SUCCESSFUL`은 증거가 아니다 —
(17)·(18)에서 빌드·테스트가 통과한 상태로 결함 7개가 남아 있었다.
⚠️ **문서·리소스만 고친 커밋에서는 테스트가 아예 안 돈다** — `UP-TO-DATE`다((41)).
`.kt`가 아닌 것을 고쳤으면 **`--rerun-tasks`를 준다.** 개수는 `TEST-*.xml`로 센다.

---

## 7. 오너 대기 중 (전부 0원)

**`구현현황_AOS.md` 8절이 원본이다.** 요약:

| | 상태 |
|---|---|
| PlantNet 키 | ✅ **주입 장치까지 끝났다** — `local.properties` → `buildConfigField` → `AppSecrets`. 실호출 확인((43)) |
| 카카오 키 | ✅ 열렸다. 오너가 **키해시를 등록**해 `vector/auth` 200 · 타일·핀 실기 확인((37)) |
| Supabase URL | ✅ `https://ngfkkazyvbbhrcznqkar.supabase.co` (iOS·AOS **같은 프로젝트**) |
| Supabase 스키마 | ✅ **적용 확인**((29)). 랭킹 함수 3개가 실제 JWT로 200 |
| Supabase provider | ✅ **kakao가 켜졌고 Manual Linking도 켜졌다**((68) 실측 — `kakao`는 302 `kauth.kakao.com`, 대조군 `google`은 400 `provider is not enabled`로 **갈렸다**) |
| **Supabase Redirect URLs** | ⏸ **오너가 "수행완료"라고 했다(2026-08-16 · (80)) — 그런데 증거가 없다.** 들어갔다는 증거는 **콜백이 완주한 logcat / 토스트** 하나뿐이고 그건 **오너 폰에서만 난다**(에뮬레이터에 카카오 앱이 없다). 🔴 **"했다"와 "확인됐다"를 같은 칸에 쓰지 않는다** → **8절** |
| ~~로그인 문구 2건~~ | ✅ **닫혔다**(2026-08-16 · (80)) — 오너가 위임해서 A 문서 3절에 표를 먼저 쓰고 `CfToast.LOGIN_LINKED`·`LOGIN_NOT_LINKED`를 넣었다. 🔴 **토스트가 실제로 뜨는 것은 오너 폰의 콜백 완주에서만 확인된다** |
| **F-1 Play 계정 유형** | ⏸ **개인 vs 사업자.** 개인은 **클로즈드 테스트 12명 × 14일 필수**, 사업자는 면제(D-U-N-S 발급에 며칠~2주). 권고는 **사업자**. ⚠️ 앱 코드는 안 바뀐다 |
| **B-3-a floor** | ✅ **0.05로 확정**((44)). **건드리지 않는다**(5절) |
| ~~**B-3-b 임계값**~~ | ✅ **닫혔다 — 현행 유지**(2026-08-16 · (80)). 오너 판단: "정답 5건 중 4건은 고르라는 거고 1건은 실패다." `0.60/0.70/0.85` 그대로이고 **코드 변경 0건**이다. 🔵 값이 안 바뀐 것이지 **안 잰 것이 아니다** — 위 실측이 그 판단의 근거였다. ⚠️ **다시 열지 않는다**(5절) |
| ~~**B-4 유사종 통합**~~ | ✅ **끝났다.** **8그룹 · 13종을 붙였고**((78) · 계약 1-6 · 0절) **남은 항목은 2026-08-16 오너가 "안 고쳐도 된다"로 닫았다**((80)) — 아래 ①② 는 **하지 않는 것으로 확정**이다(미결이 아니다 · 지우지 않는 이유는 다음 사람이 "빠뜨렸다"고 다시 손대는 것을 막기 위해서다). ⏸ **남은 것은 오너가 콘솔에서 돌릴 SQL 2절 하나뿐**이다 — 1절(미리보기)은 돌렸고 **고칠 것은 `32 서양민들레 → 31 민들레` 5행/5계정뿐**이다. ~~남은 오너 결정 2건~~: ① 국화과 노란 꽃 5종(45·46·47·194·195) 중 **어느 2종**을 남기나 ② 난이도 상 나머지 1,908종 자동 규칙(**속으로 묶으면 79%가 삼켜진다**). ⏸ 그리고 **서버 행 SQL 1건**이 오너 몫이다 — `오너_실행/05_B4_수집그룹_이관.sql`(README 6절) |
| ~~0004 SQL~~ | 🔵 **이미 들어가 있다**(2026-08-16 실측 (79) — `dong_member_count` **200**). 오너가 08-12에 합본을 다시 붙일 때 같이 적용됐고, `PGRST202`는 **08-09에 잰 낡은 값**이었다. 서버 `flowers`도 **2,057종**이다 |
| 🔴 **`0010` 함수 권한** | ⏸ **새로 생겼다**((79)) — `revoke execute … from anon`이 **아무것도 막지 않았다**(함수 생성 시 `PUBLIC`에 자동으로 붙는 EXECUTE가 남는다). 로그인 없이 `region_ranking`·`friend_ranking`·`my_season_summary`·`dong_member_count`가 **200을 준다**(실측). 오너가 `프로젝트 맥락/제안/0010_제안_함수권한_잠금.sql`을 붙여넣어야 닫힌다. 🔴 **1절(진단)은 오너가 돌렸다(2026-08-16 · (80)) — 잠글 7개가 전부 `anon=true`였다**(대조군 6개는 `true`가 정상). 즉 추론이 **실서버에서 확정됐다.** **남은 것은 2절 + 3절**이고 3절이 **13/13 ✅**여야 한다. ✅ **앱은 안 죽는다** — 익명 세션의 롤은 `authenticated`다. 🔵 **구멍·고침·"안 죽는다"를 로컬 Postgres로 전부 재현·검증했다**((79) 3-1절 · `python3 supabase/_tools/measure_0010_grants.py` → PASS) |
| 뒷정리 | ⏸ **익명 테스트 계정**을 지울 것 — 12개에서 **또 늘었다**(릴리스 앱을 지우고 다시 깔 때마다 하나씩 생긴다 · (82)). 삼성2동 시즌 참가자 **10명이 전부 나다** → ~~`제안/0011` §1~~ 🔴 **그 안내는 틀렸다((93) · 2026-09-04)**: `제안/0011`은 **탈퇴 계정용**이고 §1은 **읽기만 한다**(돌리면 `true` 4개짜리 초록 표가 뜨고 아무것도 안 지워진다). `오너_실행/03`(기록 0개)·`04`(좌표 지문)도 **이 계정들엔 안 듣는다**(실제로 찍은 기록이라 지문 밖 + 장소명이 붙어 있다) → 정리 경로는 **`출시준비_AOS.md` 4-5**이고 거기 **읽기 전용 미리보기**로 오너가 uuid를 골라 준다. 🔴 **랭킹 스크린샷은 그걸 지운 뒤에 찍는다.** ⚠️ (84) 스토어 컷 4에 그 이름 중 하나가 **그대로 찍혀 있다**(`꽃친구fbcc`) — 지도 컷이 그 자리를 대신하면 사라진다 **service_role 키는 받아서도 코드에 넣지 않는다** |
| 🚀 **출시(Play) 오너 항목** | ⏸ **`프로젝트 맥락/출시준비_AOS.md` 4절이 원본이다** — 여기 베껴 두지 않는다. 막는 것은 ~~**법적 문서 빈 칸 6종**(빌드가 멈춘다)과~~ 🔵 **(그 6칸은 2026-09-08에 닫혔다 — 남은 것은 `docs/legal/` 커밋·푸시 + 두 URL 열림 확인)** **카카오 릴리스 키해시 `YrZy5Zej88V5CuuG0jSCsXB1zZA=`**(지도만 401로 죽는다)와 **테스터 15~16명×연속 14일**(프로덕션 신청이 안 열린다). 그 밖에 심사용 카카오 계정 1개 · IARC 설문 · 키 백업 · SQL 6건 |

⚠️ **키는 소스에 넣지 않는다.** `local.properties`(gitignore됨) → `buildConfigField` →
`AppSecrets`. 규칙은 **키가 없어도 앱은 죽지 않고 그 기능만 꺼진다.**
**값을 찍거나 커밋하지 않는다** — 확인은 **이름 + 길이**로만 한다.

---

## 8. ⏸ 지금 이어서 할 일

🔵 **2026-09-08 (96): 노션 사본은 (95) 상태로 맞춰 뒀다** — 다시 옮길 것이 없다.
🔴 **노션에서 숫자를 고치지 않는다**(사본이다 · 원본은 이 폴더의 문서들이다).
⚠️ 노션을 고칠 때 **한글을 `\uXXXX`로 적지 않는다** — (94)(96) 두 번 깨졌고, 쓰는 쪽이
깨지면 **API가 성공을 주고 조용히 저장된다.** 고친 뒤에는 페이지를 **다시 받아 읽는다.**

### 8-0. 🚀 출시 — **원본은 `프로젝트 맥락/출시준비_AOS.md`다** ((82)(83) · 2026-08-17)

🔵 **내 쪽 항목은 다 닫혔다.** 스크린샷 4장을 **(83) 빌드에서 다시 찍고 눈으로 봤다**
((84) · 1080×1920 · 어긋난 일러스트 0개). 남은 흠 둘은 **오너 항목에 걸려 있다** —
4번째 칸이 지도 대신 `마이` 대타인 것과, 그 컷의 닉네임이 익명 이름(`꽃친구fbcc`)인 것
**둘 다 카카오 릴리스 키해시가 등록되면 같이 풀린다.**
⚠️ 컷들이 `1종 · 배지 0개`로 비어 보이는 것은 **고치지 않는다** — 여러 종을 모으면
일러스트 483조합 때문에 **다른 종이 같은 그림으로** 찍힌다((82) · `출시준비_AOS.md` 1절).
🔴 **나머지는 전부 오너 값·콘솔이라 여기서는 원리상 못 잰다** — 그 목록도 그 문서 4·6절이다.
⚠️ **`-PcfAllowLegalPlaceholders=true`로 만든 산출물은 절대 올리지 않는다.** 겉·크기·서명이
정상 빌드와 구분되지 않아서 **`출시/_tools/check_release_artifact.py`가 유일한 방어선**이다.
🔵 **(83)에서 그 검사가 한 층 넓어졌다** — `aapt2 dump badging`으로 **권한 집합**(네임스페이스
불문 · 늘면 데이터 보안 양식과 어긋난다)과 **필수 기능**(암시분 포함 · Play 기기 필터)을 본다.
지금 에뮬레이터·`app/build/outputs`에 있는 릴리스 산출물은 **(83) 매니페스트 수정 뒤에 다시
만든 것**이고, ~~검사에서 빨간 것은 **법적 문서 빈 칸 6줄뿐**이다(오너 값 대기 = 정상)~~
→ 🔵 **2026-09-08에 다시 만들어서 이제 빨간 줄이 0개다**(`check_release_artifact.py`
`🔵 막는 항목 없음 (경고 0개)` · AAB 88.2MB · 다운로드 58.5~64.7MB · versionCode=1).

### 8-0-a. ✅ 오너 14항목 — **내 쪽은 다 닫혔다** ((92)(93) · 2026-09-04)

| 처리 | 항목 |
|---|---|
| ✅ 구현 | 1 친구 수락 UI · 2 친구 시즌 랭킹 · 3 `친구에게만` 공개 · **6 사진 서버 업로드(A-4)** · 9+10 부정촬영 정책별 반응 · 13 만 14세 확인 |
| 🔵 닫음(안 만든다) | 7 장소 단위 핀(좌표 그대로) · 8 도감 꽃 설명문 · 14 시즌 길이(현행 유지) |
| ⏸ 오너 대기 | 4 시즌 배지·칭호 · 5 시즌 결과 화면 — **오너가 아직 안 정했다.** 손대지 않는다 |
| 🔵 이미 됨 | 11 친구 초대 |

⚠️ **항목 12는 두 원본 어디에도 이름이 없다.** 오너 원문(2026-08-27)이 12를 건너뛰었고
`오너_결정사항.md` 844행의 처리 요약에도 12가 없다 — 그 목록이 **대화에서만 만들어졌고
파일로 안 남았기 때문**이다((93) 4절과 같은 종류). 🔴 **추측해서 채우지 않는다.**
오너에게 물을 자리가 생기면 "12번이 뭐였는지"를 같이 묻는다.

🔴 **6번(A-4)에는 오너 몫이 딸려 있고 그것 없이는 사진이 한 장도 안 올라간다** —
`제안/0013`을 SQL Editor에 붙여야 버킷이 생긴다(`출시준비_AOS.md` 4-8 순서 4번).
그전까지 업로드는 **전부 404 → 보류**이고 **앱에는 증상이 0이다**(2-37).

### 8-1. **로그인 게이트 실기기 확인** ((77) · 2026-08-14 · (80)에서 갱신)

`진행.md` **(77)**이 원본이다. 코드·테스트·문서·커밋은 다 끝났고 **막힌 것은 폰 앞에서 한 번 태우는 것뿐이다.**

🔵 **2026-08-16 갱신((80)):** 오너가 Redirect URLs를 **"수행완료"라고 했다.** 그래서 8-1의
선행은 아마 끝났고, 남은 것은 **완주 확인**이다. 🔴 **여기서는 잴 수 없다** — 에뮬레이터에
카카오 앱이 없다. **오너 폰에서 로그인 한 번 → 토스트가 뜨는지**가 유일한 증거다.
그리고 ⓒ의 문구는 **이제 있다**(`카카오 계정으로 로그인했어요` / `로그인이 완료되지 않았어요`).

🔵 **(77) 이후 (78)에서 B-4 수집 그룹을 붙였다**(계약 1-6 · 유료 호출 0건). 그건 콘솔과
무관하게 끝났으므로 **이 절은 그대로 남는다** — 다만 오너에게 줄 것이 **하나 늘었다**:
`오너_실행/05_B4_수집그룹_이관.sql`(1절 미리보기만 열려 있다 · `오너_실행/README.md` 6절).

### 8-1-a. 선행 — 오너가 Supabase 콘솔에 한 줄

```
Authentication → URL Configuration → Redirect URLs ← catchflower://auth-callback
```

🔴 **없으면 서버가 오류 대신 Site URL로 조용히 보낸다** — 로그에도 안 남고
**밖에서는 잴 수 없다**(GoTrue `state`가 불투명 uuid라 대조군과 답이 같았다).
**실제 콜백을 한 번 완주하는 것만이 증거다.**

그다음 두 가지를 눈으로 본다:

| | 무엇 | 주의 |
|---|---|---|
| ⓐ | 시트 → 카카오 → 복귀 **완주** → `markKakaoLinked`가 실제로 불린다 | 🔴 **인텐트가 온 것으로 판정하지 않는다** — 실패도 같은 주소로 온다(`#error=…`) |
| ⓑ | **3회째 셔터에서 시트가 뜬다** | ⚠️ 판별로 확인하면 성공 2건이 필요해 **PlantNet 쿼터를 쓴다** → **댓글·좋아요·신고(0회)가 싸다** |
| ⓒ | 성공했을 때의 화면 | ✅ **문구가 붙었다**(2026-08-16 · (80)) — 성공 `카카오 계정으로 로그인했어요` · 실패·취소 `로그인이 완료되지 않았어요`. 🔴 **토스트가 유일한 표시다** — 카카오에서 돌아와도 화면은 로그인 전과 똑같다(게이트가 풀린 것은 다음 액션에서 보인다) |

⚠️ **게이트를 부르는지는 JVM에서 못 잰다.** `onPhotoTaken`에서 호출을 지워도
`LoginGateTest` 12개가 전부 초록이다(`CaptureViewModel`은 `Application`이 필요하다).
**그래서 이 절이 있다.**

🔵 **2026-08-16((81)): ⓑ의 싼 길로 절반을 봤다.** 에뮬레이터(익명 상태 확인 뒤)에서
화면 20 `프로필 수정` · 화면 19 `친구 추가`를 눌러 **시트가 뜨고 화면 20-1이 안 열리고
`친구 추가`가 `요청 보냄`으로 안 바뀌는 것**을 확인했다(유료 호출 0건).
🔴 **셔터 경로는 그대로 남는다** — 판정 함수는 같지만 **부르는 자리가 다르다.**

### 8-1-b. 화면 12의 원인을 귀속하는 법 (재사용 도구 · (46))

폰을 꽂으면 촬영 로그가 남아 있다(버퍼 비우고 스팸 태그를 침묵시켜 뒀다).

```bash
adb -s R5CX21QNVVN logcat -d -s CatchFlower
```

**네 줄을 순서대로 읽어 원인을 귀속한다** — 화면에는 아래 셋이 **똑같이**
`어떤 꽃인지 알 수 없었어요`로 보인다:

```
1차필터: 꽃=true top=… conf=…              ← false면 유료 호출 0건(과금 안 됨)
판별 호출 직전: PlantNetRecognizer 후보=98개 (8월)   ← N>0이면 그 촬영은 과금됐다
판별 응답: 받은 후보=5 […학명 점수…] → 통과=2 […] (floor=0.05)
화면 12: 통과 후보=2 1순위점수=0.018 연속실패=1
```

| | 읽는 법 | 원인 | 우리 층인가 |
|---|---|---|---|
| ⓐ | `받은 후보=0` | PlantNet이 아무것도 못 알아봤다 | 아니다 (API) |
| ⓑ | `받은 후보=N` · `통과=0` | 도감(지금 2,057종)·개화월에서 전부 걸렸다 | **우리 도감** (B-4) |
| ⓒ | `통과=N`인데 화면 12 | 1순위가 floor 미달 | **우리 정책** (B-3-a) |

**성공했으면** 화면 09/10/11로 가고 그 뒤 화면 13(지도 공유) → 화면 14 핀까지가 QA 대상이다.

⚠️ **QA 우회 스위치는 껐다** — 실물 꽃에는 필요 없다(4-1).
⚠️ **화면 사진이 floor에 걸린 것을 "floor가 높다"로 읽지 않는다.** 실기기 실물 민들레는
0.112·0.185였고 화면 사진은 **0.018**이었다 — 1차 필터도 그것을 `Textile`로 읽었다.
**표본의 한계이지 정책 결함이 아니다**((46)).

**스위치 상태(2026-08-14 에뮬레이터에서 확인):** `/data/local/tmp/cf_*`가 **하나도 없다** —
QA 우회 off · `cf_run_network_tests` 없음. **켠 채로 잊은 것이 없다.**

**폰을 꽂기 전 상태(2026-08-09에 확인해 둔 것):** QA 스위치 off · `cf_run_network_tests` 없음 ·
`View`/`VRI`/`OpenGLRenderer`/`HWUI` 태그 `SILENT`(`persist.log.tag.*` · 재부팅에도 유지) ·
새 APK 설치됨(dex에서 새 로그 문자열 확인) · 로그 버퍼 비움.
🔴 **logcat 링버퍼 상한은 5MiB다** — 크기로는 못 늘린다(`setprop persist.logd.size 16M` 실패).
Compose가 초당 수십 줄을 쏟아 **촬영 흔적이 밀려 나간 적이 있다.** 그래서 태그를 침묵시켰다.

기기: **`R5CX21QNVVN`** (SM-A256N · Android 16 / API 36 · arm64-v8a).

---

## 9. 제출 상태 — 저장소가 공개돼 있다 (2026-08-10 · 48차)

🔴 **git 원격이 생겼다.** 이 문서는 8절까지 "원격이 없다"고 말했었다 — 지금은 push된다.

| | |
|---|---|
| 저장소 | **https://github.com/Aaron-HyoSan/catchflower** (공개 · `main`) |
| 커밋 신원 | `Aaron-HyoSan` / `310650246+Aaron-HyoSan@users.noreply.github.com` **(Vercel 때와 같은 이유로 형식이 정해져 있다)** |
| APK | Release **v1.0** · `catchflower-v1.0.apk` (110,035,173 bytes) — **100MB를 넘어 Release 자산으로만 올라간다** |
| 소개 페이지 | **https://aaron-hyosan.github.io/catchflower/** (`main /docs`) |
| 제출 문서 | `제출물/` — 게임 소개 · AI 활용 · 팀원 롤 양식 · 영상 콘티 (**PDF는 생성물**) |

⚠️ **배포 APK는 debug 서명이다. release 키로 다시 서명하면 지도만 조용히 죽는다** —
카카오 콘솔에 등록된 키해시가 이 맥의 **debug 키스토어 SHA-1**(`Vbb1YnBobsq+CLwfh3xnNgVs6a8=`)이고,
불일치하면 `vector/auth`가 **401**을 주는데 화면에는 회색 배경만 보인다((35)(37)).

⚠️ **`제출물/*.pdf`를 손으로 고치지 않는다.** `python3 제출물/_tools/build_pdf.py`가 원본이고
`test_build_pdf.py`가 변환기를 검사한다. **이미지 경로가 틀려도 PDF는 정상 생성되고 그 칸만 빈다**
— 그래서 빌드 전에 이미지 존재를 확인하고, 만든 뒤에는 **페이지를 눈으로 읽어 확인**한다.

🔴 **제출 문서를 다시 낼 때는 수치를 다시 센다((50)).** 회차·줄 수·커밋 수·테스트 개수가
낡은 채로 PDF 2개에 박혀 있었다. 그중 **계측 "47개"는 낡은 게 아니라 처음부터 틀린 값**이었다
(`@Test`는 45개). 세는 명령은 `구현현황_AOS.md` 0-1절에 있다.
🔴 **`**굵게**`를 줄바꿈에 걸치지 않는다** — 변환기가 목록의 이어진 줄을 합치지 않아서
`**`가 PDF에 그대로 찍히고 **뒷부분이 불릿 밖으로 떨어진다.** 둘 다 "생성 성공"이다.

### 9-1. 🎬 플레이 영상 — **찍었다** ((49) · 2026-08-10)

| | |
|---|---|
| 제출용 | **`제출물/영상/catchflower_play_51s.mp4`** · 51.0초 · 3,580,868 bytes (요건 30~60초) |
| 원본 | `제출물/영상/catchflower_play.mp4` · 157.0초 (자르기 전 전체 흐름) |
| 다시 만들기 | `python3 제출물/_tools/record_play_video.py --setup` → `… trim_play_video.py` |
| 비용 | **PlantNet 1건**(누적 23건/500·일) · 앱 코드 변경 0 · QA 우회 스위치 **안 켰다** |

**에뮬레이터 `virtualscene` 카메라가 3D 방 벽에 사진을 걸어 주고, 앱은 그것을 진짜
카메라 파이프라인으로 촬영한다** — 1차 필터·개화월 압축·PlantNet 실호출이 전부 실제로 돈다.
🔵 **이 문서와 콘티가 "에뮬레이터로는 못 찍는다"고 말했던 것은 틀렸다.**

🔴 **다시 찍을 일이 있으면 함정 6개를 먼저 읽는다 — 전부 증상이 없다.**
`진행.md` (49) 표가 원본이고 스크립트 docstring에도 있다. 특히:

| | |
|---|---|
| **AVD `config.ini`가 CLI 플래그를 이긴다** | `hw.camera.back`·**`front` 둘 다** 고쳐야 카메라가 붙는다. 안 하면 검은 프리뷰 + 비활성 셔터뿐이다 |
| **컷 초과가 보이지 않는다** | `--time-limit`은 컷이 남아도 끝내고 스크립트는 "완주"를 찍는다. 실측 랭킹 컷 9.8초 잘림 |
| **자른 영상의 "성공"도 증거가 아니다** | 첫 편집본은 초록이었는데 **분석중 컷이 빠지고 검은 프리뷰가 붙어 있었다.** 반드시 프레임 시트를 눈으로 읽는다 |

⚠️ **AVD config는 원복해 뒀다.** 다시 녹화하려면 `--setup`이 필요하다
(백업: `~/.android/avd/CatchFlower_Pixel8.avd/config.ini.before_virtualscene`).

🔵 **유튜브 업로드는 오너가 접었다 — 다시 꺼내지 않는다.** (이 줄은 예전에 "사람이 할 일
하나"로 적혀 있었다. 남겨 두면 세션마다 다시 물어보게 되므로 여기서 닫는다.)
⚠️ 그래도 콘티 5-2절 설명란에 `Galaxy A25 실기기에서 실제 꽃을 촬영해 녹화한 것입니다`가
남아 있는데 **이 영상은 에뮬레이터다.** 어딘가에 다시 올릴 일이 생기면 그 줄을 먼저 고친다.

---

## 10. 🔌 재부팅했으면 — 되살릴 것과 착각할 것 ((77))

**끄면 사라지는 것:** 헤드리스 에뮬레이터 · Gradle 데몬 · adb 서버 ·
`$CLAUDE_JOB_DIR/tmp`의 임시 파일(**전부 재생성물이다**).

```bash
emulator -avd CatchFlower_Pixel8 -no-window -no-audio &   # 40초
adb wait-for-device shell getprop sys.boot_completed        # 1이면 준비 완료
```

⚠️ 환경변수는 `~/.zshrc`에 박혀 있어 **재부팅과 무관하다**(2절).

**안 사라지는데 착각하기 쉬운 것:**

| | |
|---|---|
| 미커밋 **704개** | **703개는 iOS 터미널의 일러스트 diff고 내 것이 아니다** → 🔴 **`git add -A .`를 절대 쓰지 않고 파일 목록을 명시한다** |
| `main`이 **ahead**(개수는 그때 센다) | 재부팅으로 사라지지는 않지만 **이 맥 밖에는 없다** |
| `오너_실행/02_적용확인.sql` · `04_테스트계정_삭제.sql` | ⏸ **잘린 채 modified다. 오너만 복원할 수 있다** |
| AVD `config.ini` | **원복돼 있다.** 영상을 다시 찍으려면 `--setup`이 필요하다 — 🔴 **config가 CLI 플래그를 이긴다**(9-1절) |
