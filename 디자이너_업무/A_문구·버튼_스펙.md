# A. 와이어프레임 문구 · 버튼 스펙

> 캐치플라워 앱에 들어가는 **모든 화면 문구와 버튼 텍스트**의 확정안.
> 와이어프레임(`와이어프레임/*.svg`)에 실제로 그려진 문구와 1:1 일치한다.
> 디자인 작업 시 이 문서의 문구를 그대로 사용하고, 임의로 줄이거나 바꾸지 않는다.

---

## 0. 문구 작성 원칙

주 타깃이 **40~50대 여성**이라는 점이 모든 문구 판단의 기준이다.

| 원칙 | 하지 않는다 | 한다 |
|---|---|---|
| 게임 용어를 앞세우지 않는다 | 퀘스트, 미션, 랭킹업, 컬렉션 | 꽃 모으기, 순위, 도감 |
| 시스템 어투를 쓰지 않는다 | "인식 실패", "처리 중", "오류 발생" | "알 수 없었어요", "보고 있어요" |
| 실패를 사용자 잘못으로 만들지 않는다 | "잘못 촬영하셨습니다" | "다시 한 번 찍어 주시겠어요?" |
| 동작을 지시한다 | "적절한 거리에서 촬영" | "네모 안에 꽉 채워 주세요" |
| 규칙은 이득으로 설명한다 | "시즌 점수 증가 없음" | "사진은 도감에 쌓여요" |
| 외래어는 대중 인지명만 | 컬렉션, 피드, 프로필 편집 | 도감, 기록, 프로필 수정 |
| 숫자는 단위까지 | "37" | "37종", "4회", "47일 남음" |

**어미 통일** — 안내문은 `~어요/~아요` (부드러운 구어), 버튼은 명사형 또는 `~기` (`공유하기`, `다시 찍기`).
경어 `~하십시오`, `~하시겠습니까` 는 쓰지 않는다.

---

## 1. 버튼 · 텍스트 스타일 정의

디자이너가 컴포넌트로 만들어야 하는 최소 단위.

### 버튼

| 종류 | 용도 | 사양 | 예시 |
|---|---|---|---|
| Primary | 화면의 주 행동, 화면당 1개 | 높이 **56px**, 라운드 완전(28px), 채움, 굵게 15~16pt | `네, 맞아요` `공유하기` |
| Secondary | 대안 행동 | 높이 **54~56px**, 테두리 1.5px, 흰 배경, 굵게 15~16pt | `아니에요, 다시 찍을게요` |
| Ghost | 회피·보조 | 높이 50px, 연한 회색 배경, 중간 굵기 15pt | `나만 보기` `나중에 할게요` |
| Small | 카드 내부 | 높이 32~40px, 12~13.5pt | `길찾기` `추가` `초대하기` |
| Text | 최소 강조 | 굵게 13pt, 밑줄 없음 | `공유하지 않기` `전체 보기` |
| Disabled | 조건 미충족 | 회색 배경 + 회색 글자, 문구를 안내로 교체 | `동네를 선택해 주세요` |

- **최소 터치 영역 44×44px**. 아이콘 단독 버튼도 이 값을 지킨다.
- 아이콘만 있는 버튼은 만들지 않는다. **반드시 텍스트를 병기**한다 (`좋아요 12`).

### 텍스트

| 역할 | 크기 | 굵기 | 용도 |
|---|---|---|---|
| 화면 제목 | 19~21pt | Bold | 헤더 `내 꽃 도감` |
| 축하/질문 대상 | 24~32pt | Bold | `장미`, `연남동 4위` |
| 섹션 제목 | 13.5~14.5pt | Bold | `최근 발견한 꽃` |
| 본문 | 12~13.5pt | Regular | 설명문 |
| 보조 설명 | 10.5~11.5pt | Regular | 회색 부연 |
| 최소 크기 | **10.5pt** | - | 이보다 작게 쓰지 않는다 |

> 노안을 고려해 본문 최소 12pt를 권장한다. 10.5pt는 날짜·개수 등 보조 정보에만 쓴다.

---

## 2. 화면별 문구 전체

### 01 로그인 — `01_로그인.svg`
| 위치 | 문구 |
|---|---|
| 서비스명 | 캐치플라워 |
| 태그라인 | 산책길에 만난 꽃, 사진으로 모으는 나만의 꽃 도감 |
| 버튼 1 (Primary) | 카카오로 3초 만에 시작하기 |
| 버튼 2 (Secondary) | 애플로 시작하기 |
| 버튼 3 (Secondary) | 휴대폰 번호로 시작하기 |
| 약관 | 시작하면 이용약관과 개인정보 처리방침에 / 동의한 것으로 봅니다. |
| 링크 | 이용약관 · 개인정보 처리방침 |

### 02 활동 지역 선택 — `02_지역선택.svg`
| 위치 | 문구 |
|---|---|
| 헤더 | 활동 지역 선택 (우측 `1/2`) |
| 제목 | 어느 동네에서 활동하세요? |
| 설명 1 | 같은 동네 이웃들과 꽃 수집 순위를 겨루게 됩니다. |
| 설명 2 (강조) | 가입 후에는 6개월에 한 번만 변경할 수 있어요. |
| 검색창 | 동 이름으로 검색 (예: 연남동) |
| 버튼 (Secondary) | 현재 위치로 우리 동네 찾기 |
| 목록 라벨 | 검색 결과 |
| 목록 항목 | 서울특별시 마포구 연남동 / 이웃 1,284명 활동 중 |
| CTA (Primary) | **{동명}으로 시작하기** — 미선택 시 `동네를 선택해 주세요` (Disabled) |
| 하단 고지 | 선택한 동네는 {날짜}부터 변경할 수 있어요. |
| 이웃 10명 미만 | 아직 이웃이 적어요 |

### 03 권한 안내 — `03_권한안내.svg`
| 위치 | 문구 |
|---|---|
| 헤더 | 권한 안내 (우측 `2/2`) |
| 제목 | 이 세 가지만 허용하면 준비 끝! |
| 설명 | 허용하지 않아도 도감은 쓸 수 있지만 일부 기능이 제한돼요. |
| 카메라 `필수` | 꽃을 직접 촬영해 도감에 등록합니다. / 앨범 사진은 등록할 수 없어요. |
| 위치 `선택` | 꽃을 발견한 장소를 지도에 남깁니다. / 끄면 지도 공유를 쓸 수 없어요. |
| 연락처 `선택` | 이미 가입한 지인을 친구로 연결합니다. / 번호는 암호화해 보관하며 저장하지 않아요. |
| 안심 박스 | 촬영한 사진은 내 도감에만 저장됩니다. / 지도 공유는 매번 직접 선택해요. |
| CTA (Primary) | 허용하고 시작하기 |
| 하단 | 나중에 설정에서 바꿀 수 있어요 |

### 04 도감 홈 — `04_도감홈.svg`
| 위치 | 문구 |
|---|---|
| 헤더 | 내 꽃 도감 |
| 현황 카드 | 모은 꽃 · **37** / 200종 · 이번 시즌 **12종** |
| 진행 문구 | 도감 18% 완성 · 다음 배지까지 3종 |
| 섹션 1 | 최근 발견한 꽃 (우측 `전체 보기`) |
| 섹션 2 | 전체 200종 (우측 `필터`) |
| 필터 칩 | 전체 / 모은 꽃 / 봄 / 여름 / 가을 |
| 미발견 셀 | 미발견 |
| 하단 내비 | 도감 · 지도 · **꽃 촬영** · 랭킹 · 마이 |

### 05 도감 상세 — `05_도감상세.svg`
| 위치 | 문구 |
|---|---|
| 헤더 | {꽃이름} (우측 `공유`) |
| 학명 줄 | Rosa hybrida · 장미과 |
| 속성 칩 | 여름 / 흔함 / 붉은색 |
| 지표 3칸 | 발견 횟수 `4회` · 첫 발견 `5월 2일` · 장소 `3곳` |
| 섹션 | 꽃 이야기 |
| 비슷한 꽃 | 비슷한 꽃 · 해당화, 찔레꽃 |
| 섹션 | 내 발견 기록 (우측 `4회`) |
| 기록 배지 | 공개 / 비공개 |

### 06 도감 필터 — `06_도감필터.svg`
| 위치 | 문구 |
|---|---|
| 시트 제목 | 필터 (우측 `초기화`) |
| 그룹 1 | 수집 여부 — 전체 / 모은 꽃 / 미발견 |
| 그룹 2 | 계절 — 봄 / 여름 / 가을 / 겨울 |
| 그룹 3 | 색상 — 흰색 / 노랑 / 분홍 / 붉은색 / 보라 / 파랑 |
| 그룹 4 | 보기 쉬움 — 흔함 / 보통 / 귀함 |
| CTA (Primary) | **{n}종 보기** — 0종이면 `조건에 맞는 꽃이 없어요` (Disabled) |

### 07 카메라 — `07_카메라.svg`  *(어두운 화면)*
| 위치 | 문구 |
|---|---|
| 헤더 | 꽃 촬영 (좌 `닫기` / 우 `도움말`) |
| 가이드 상단 | 꽃 한 송이를 네모 안에 꽉 채워 주세요 |
| 가이드 하단 | 너무 멀면 잘 못 알아봐요 |
| 앨범 고지 | 앨범 사진은 등록할 수 없어요. 직접 찍어 주세요. |
| 셔터 라벨 | 찍기 |
| 보조 | 플래시 / 전환 |

### 08 AI 분석 중 — `08_AI분석중.svg`  *(어두운 화면)*
| 위치 | 문구 |
|---|---|
| 제목 | 어떤 꽃인지 보고 있어요 |
| 소요 시간 | 5초 정도 걸려요 |
| 10초 초과 | 조금 더 걸리고 있어요 |
| 하단 | 취소 |

### 09 AI 판별 결과 — `09_판별확인.svg`
| 위치 | 문구 |
|---|---|
| 질문 (3줄) | 이 꽃은 / **{꽃이름}** / 인가요? |
| 부연 | 장미과 · 5~6월에 피는 꽃 |
| 버튼 (Primary) | 네, 맞아요 |
| 버튼 (Secondary) | 아니에요, 다시 찍을게요 |
| 힌트 | 비슷한 꽃 · 해당화, 찔레꽃 / 다르면 다시 찍어 주세요 |

### 10 신규 꽃 등록 — `10_신규등록.svg`
| 위치 | 문구 |
|---|---|
| 축하 (2줄) | 새로운 꽃을 발견했어요! / {꽃이름}가 도감에 등록되었습니다. |
| 배지 | {n}번째 꽃 |
| 지표 | 도감 `37 / 200종` · 이번 시즌 `13종 (+1)` |
| 순위 변동 | {동명} 순위 24위 → 21위 (우측 `보기`) |
| 버튼 (Primary) | 지도에 공유하기 |
| 버튼 (Ghost) | 나만 보기 |

> 조사 처리 주의: `{꽃이름}가/이` — 받침 유무에 따라 분기해야 한다. (`장미가` / `개망초가` / `국화가` vs `억새가`… 받침 있으면 `이`) 개발 시 조사 자동 처리 함수 필요.

### 11 기존 꽃 재발견 — `11_재발견.svg`
| 위치 | 문구 |
|---|---|
| 축하 (2줄) | {꽃이름}를 다시 발견했어요! / 이번이 **{서수}** 발견입니다. |
| 서수 표기 | 두 번째 ~ 열한 번째 까지 한글, 12회 이상은 `12번째` |
| 기록 카드 | 이번 발견 기록 — 날짜 / 장소 / 총 발견 |
| 안내 박스 | 이번 시즌 종수는 늘지 않아요 / 같은 꽃은 한 종으로 계산해요. 사진은 도감에 쌓여요. |
| 사진 스트립 | 지금까지 만난 {꽃이름} (최신 사진에 `NEW`) |
| 버튼 | 지도에 공유하기 / 나만 보기 |

### 12 AI 판별 실패 — `12_판별실패.svg`
| 위치 | 문구 |
|---|---|
| 제목 | 어떤 꽃인지 알 수 없었어요 |
| 설명 | 다시 한 번 찍어 주시겠어요? |
| 팁 제목 | 이렇게 찍으면 잘 알아봐요 |
| 팁 4개 | 꽃 한 송이가 화면에 꽉 차게 / 그림자 없는 밝은 곳에서 / 정면이나 살짝 위에서 / 흔들리지 않게 잠시 멈춰서 |
| 사진 하단 | 이 사진은 저장되지 않았어요 |
| 버튼 (Primary) | 다시 찍기 |
| 버튼 (Ghost) | 나중에 할게요 |
| **3회 연속 실패** | 제목을 `꽃이 아닐 수도 있어요`로 교체 + 도감 홈 복귀 유도 |

### 13 지도 공유 설정 — `13_지도공유설정.svg`
| 위치 | 문구 |
|---|---|
| 헤더 | 지도에 공유하기 |
| 제목 | 이 꽃을 지도에 공유할까요? |
| 설명 | 공유하면 다른 사람이 이 장소에서 꽃을 찾아볼 수 있어요. |
| 장소 섹션 | 발견 장소 (우측 `변경`) |
| 장소 부연 | 성동구 성수동1가 · 정확한 위치는 공개되지 않아요 |
| 공개 범위 | 누구에게 보여줄까요? |
| 옵션 1 | 모두에게 공개 / 지도를 보는 누구나 볼 수 있어요 |
| 옵션 2 | 친구에게만 공개 / 연락처로 연결된 친구만 볼 수 있어요 |
| 한 줄 섹션 | 한 줄 남기기 (안 써도 돼요) |
| 입력 예시 | 예: 숲길 끝 벤치 옆에 활짝 피었어요 |
| 글자 수 | 0 / 40 |
| 버튼 (Primary) | 공유하기 |
| 버튼 (Text) | 공유하지 않기 |
| 토스트 | 도감에는 저장됐어요 |

### 14 지도 홈 — `14_지도홈.svg`
| 위치 | 문구 |
|---|---|
| 검색창 | 공원, 산책로 이름으로 찾기 |
| 필터 칩 | 전체 / 친구 / 이번 주 / 못 모은 꽃 |
| 프리뷰 카드 | 서울숲 · `1.2km` / 꽃 5종 · 기록 12개 / 장미, 개망초, 금계국 외 2종 |
| 버튼 | 길찾기 / 자세히 보기 |
| 플로팅 | 내위치 / 목록 |

### 15 장소 상세 — `15_장소상세.svg`
| 위치 | 문구 |
|---|---|
| 장소명 | 서울숲 / 성동구 성수동1가 · 1.2km |
| 버튼 (Primary) | 길찾기 |
| 지표 3칸 | 꽃 종류 `5종` · 기록 `12개` · 이번 주 `3개` |
| 섹션 | 이곳에서 발견된 꽃 (우측 `내 도감 기준`) |
| 범례 | ! 표시는 내 도감에 없는 꽃이에요 |
| 섹션 | 사람들의 기록 (우측 `최신순 ▾`) |

### 16 꽃 기록 상세 — `16_기록상세.svg`
| 위치 | 문구 |
|---|---|
| 헤더 | 꽃 기록 (우측 `신고`) |
| 작성자 | 꽃보다효산 / 연남동 · 2시간 전 |
| 버튼 | 친구 추가 / 도감에서 보기 |
| 반응 | 좋아요 12 · 댓글 3 |
| 입력창 | 댓글을 남겨보세요 (버튼 `등록`) |

### 17 랭킹 · 우리 동네 — `17_랭킹_지역.svg`
| 위치 | 문구 |
|---|---|
| 헤더 | 랭킹 (우측 `지난 시즌`) |
| 탭 | 우리 동네 / 친구 |
| 시즌 배너 | 2026 시즌 2 / {동명} 꽃 수집 순위 / 9월 30일 마감 · 47일 남음 / `47` 일 남음 |
| 내 순위 카드 | 내 순위 `21위` `▲ 3` / 이번 시즌 모은 꽃 `13종` / 3종만 더 모으면 15위권! |
| 목록 라벨 | {동명} 이웃 1,284명 |
| 행 부연 | 대표 꽃 · 장미 |
| 더 보기 | 6위부터 더 보기 |

### 18 랭킹 · 친구 — `18_랭킹_친구.svg`
| 위치 | 문구 |
|---|---|
| 헤더 우측 | 친구 관리 |
| 제목 | 친구 8명과 겨루는 중 |
| 내 순위 | 나 (꽃보다효산) / {1위 닉네임}까지 25종 남음 |
| 초대 유도 | 친구가 많을수록 재미있어요 / 연락처에 저장된 지인을 초대해 보세요 (버튼 `초대`) |
| **친구 0~2명** | 아직 겨룰 친구가 없어요 → 초대 화면으로 전체 대체 |

### 19 친구 관리 · 초대 — `19_친구관리.svg`
| 위치 | 문구 |
|---|---|
| 헤더 | 친구 (우측 `검색`) |
| 탭 | 내 친구 {n} / 초대하기 |
| 안내 박스 | 연락처에 캐치플라워 이웃 {n}명이 있어요 / 친구로 추가하면 순위를 함께 볼 수 있어요 |
| 섹션 1 | 연락처에서 찾은 친구 (버튼 `추가`) |
| 섹션 2 | 아직 가입하지 않은 지인 (버튼 `초대하기`) |
| 번호 표기 | 010-2••••-1234 (마스킹 필수) |
| CTA (Primary) | 초대 링크 보내기 |

### 20 마이페이지 — `20_마이페이지.svg`
| 위치 | 문구 |
|---|---|
| 헤더 | 마이 (우측 `설정`) |
| 프로필 | 꽃보다효산 / `우리 동네 꽃박사` / 연남동 · 2026년 3월부터 함께 |
| 버튼 | 프로필 수정 |
| 지표 3칸 | 모은 꽃 `37종` · 총 발견 `112회` · 공유 `26개` |
| 섹션 | 내 배지 (우측 `3개 · 전체 보기`) |
| 활동 지역 | 활동 지역 / 서울특별시 마포구 연남동 / `변경 불가` · 2027. 2. 4. 부터 가능 |
| 메뉴 | 친구 관리 `8명` / 내가 공유한 꽃 `26개` / 지난 시즌 기록 / 알림 설정 / 고객문의 |

### 21 시즌 종료 결과 — `21_시즌결과.svg`
| 위치 | 문구 |
|---|---|
| 상단 | 2026 시즌 1이 끝났어요 |
| 결과 | {동명} 4위 / 이웃 1,284명 중 |
| 칭호 | {칭호명} / 칭호를 받았어요 |
| 기록 카드 | 시즌 1 기록 — 모은 꽃 `24종` / 발견 횟수 `68회` / 지도 공유 `19개` / 최고 순위 `3위` |
| 안내 박스 | 도감은 그대로 남아요 / 모은 꽃 37종과 사진은 초기화되지 않아요. / 순위 점수만 새 시즌으로 리셋됩니다. |
| 버튼 (Primary) | 시즌 2 시작하기 |
| 버튼 (Ghost) | 결과 공유하기 |

### 22 빈 상태 — `22_빈상태.svg`
| 위치 | 문구 |
|---|---|
| 현황 | 모은 꽃 `0` / 200종 |
| 제목 | 아직 모은 꽃이 없어요 |
| 설명 | 산책길에 만난 꽃을 찍어 첫 칸을 채워보세요 |
| 버튼 (Primary) | 꽃 찍어보기 |
| 추천 섹션 | 처음이라면 이 꽃부터 / 지금 이 계절, 동네에서 흔히 보이는 꽃이에요 |

---

## 3. 공통 문구 (와이어프레임 외)

디자인에는 없지만 개발에 필요한 문구. 디자이너는 **토스트·다이얼로그 컴포넌트**만 만들면 된다.

### 토스트
| 상황 | 문구 |
|---|---|
| 도감 등록 후 공유 안 함 | 도감에는 저장됐어요 |
| 지도 공유 완료 | 지도에 공유했어요 |
| 친구 추가 완료 | {이름}님과 친구가 되었어요 |
| 초대 발송 | 초대를 보냈어요 |
| 좋아요 취소 | 좋아요를 취소했어요 |
| 신고 접수 | 신고를 접수했어요. 확인 후 처리됩니다. |
| 네트워크 오류 | 연결이 불안정해요. 잠시 후 다시 시도해 주세요. |

### 아직 안 만든 기능을 눌렀을 때 (2026-08-09 추가 · AOS 세션)

| 상황 | 문구 |
|---|---|
| 예선 범위 밖 기능 | 아직 준비 중이에요 |

- **왜 필요한가.** 예선 범위에서 **버튼 10개가 눌려도 아무 일도 안 한다**(설정 ·
  프로필 수정 · 알림 설정 · 고객문의 · 검색 · 초대 링크 보내기 · 더 보기 · 전체 보기 ·
  내가 공유한 꽃 · 이미지 카드 공유). 코드에는 `TODO`가 달려 있지만 **화면에는 아무
  표시가 없다** — 누른 사람은 **앱이 고장난 것으로 읽는다.** 시연에서 심사위원이
  누르는 것이 대부분 이 버튼들이다.
- ⚠️ **버튼을 지우는 것보다 낫다.** 와이어프레임에 있는 버튼을 빼면 화면이 스펙과
  달라지고, 나중에 붙일 자리도 사라진다. 반대로 `Disabled`로 두면 **왜 못 누르는지**
  설명이 없다.
- ⚠️ **`준비 중입니다`가 아니라 `아직 준비 중이에요`다.** 0절 톤 규칙(`~해요` 종결)과
  3절 다른 문구들의 말투를 따른다.
- 🔴 **이 문구를 "구현했다"의 표시로 쓰지 않는다.** 토스트가 뜨는 것은 기능이 아니라
  **기능이 없다는 안내**다. `구현현황_AOS.md`에 ✅를 주지 않는다.

### 권한 재요청 시트
| 권한 | 문구 |
|---|---|
| 카메라 | 꽃을 촬영하려면 카메라 권한이 필요해요 / 설정에서 카메라를 켜주세요 (버튼 `설정으로 이동` / `나중에`) |
| 위치 | 지도에 남기려면 위치 권한이 필요해요 / 도감 등록은 그대로 할 수 있어요 |
| 연락처 | 지인을 찾으려면 연락처 권한이 필요해요 / 번호는 저장하지 않아요 |

### 확인 다이얼로그
| 상황 | 문구 |
|---|---|
| 촬영 중 이탈 | 촬영을 그만할까요? / 찍은 사진은 저장되지 않아요 (`그만하기` / `계속 찍기`) |
| 기록 삭제 | 이 발견 기록을 지울까요? / 지우면 되돌릴 수 없어요 (`지우기` / `취소`) |
| 공유 취소 | 지도에서 내릴까요? / 도감 기록은 그대로 남아요 (`내리기` / `취소`) |

### 빈 상태 (화면 22 외)
| 화면 | 문구 |
|---|---|
| 지도에 기록 없음 | 아직 이 근처에 공유된 꽃이 없어요 / 첫 번째로 남겨보세요 |
| 친구 없음 | 아직 겨룰 친구가 없어요 / 연락처에서 지인을 찾아보세요 |
| 필터 결과 0 | 조건에 맞는 꽃이 없어요 / 필터를 바꿔보세요 |
| 배지 없음 | 아직 받은 배지가 없어요 / 시즌이 끝나면 받을 수 있어요 |

### B-5 하루 중복 안내 (2026-08-05 추가 · iOS 세션)

**왜 추가했나.** B-5(같은 종 + 같은 장소는 하루 1회)는 오너가 확정한 규칙인데
**이 문서에 문구가 없었다.** 규칙은 있고 사용자에게 할 말이 없으면 개발이 막힌다.
0절 원칙(실패를 사용자 잘못으로 만들지 않는다 · 이유와 대안을 같이 준다)에 맞춰 썼다.
**오너 검토 대상이다.**

| 요소 | 문구 |
|---|---|
| 제목 | 오늘 여기서 만난 꽃이에요 |
| 설명 | 같은 자리에서 같은 꽃은 하루에 한 번 기록해요. |
| 설명 2 | 장소를 옮기면 다시 기록할 수 있어요. |
| 버튼 (주) | 도감에서 보기 |
| 버튼 (보조) | 다른 꽃 찍기 |

**"어제는 됐는데 오늘은 안 되는" 상황이 아니라는 걸 분명히 한다** —
막는 게 아니라 **이미 기록됐다**는 뜻이라서 제목을 부정형으로 쓰지 않았다.

### B-3 어뷰징 가드 ① 희귀종 추가 확인 (2026-08-05 추가 · iOS 세션)

**왜 추가했나.** B-3 대응 1번(`희귀종을 낮은 순위에서 고르면 즉시 확정하지 않고
사진을 한 장 더 받는다`)은 오너 확정 규칙인데 **문구가 없어서 구현이 멈춰 있었다.**

**가장 조심해야 하는 문구다.** 실제로 귀한 꽃을 만난 사람이 대다수이고,
어뷰징하는 사람은 소수다. **다수를 의심하는 말투가 되면 안 된다** —
0절 원칙 `실패를 사용자 잘못으로 만들지 않는다`가 여기서 가장 강하게 걸린다.
그래서 "확인이 필요합니다"가 아니라 **"귀한 꽃이라 한 장 더"**로 썼다.
검증이 아니라 **대접**으로 읽히게 한다. **오너 검토 대상이다.**

| 요소 | 문구 |
|---|---|
| 제목 | 귀한 꽃이네요! |
| 설명 | {꽃이름}은 보기 드문 꽃이라 사진을 한 장 더 받아요. |
| 설명 2 | 조금 다른 각도에서 찍어 주시면 좋아요. |
| 버튼 (주) | 한 장 더 찍기 |
| 버튼 (보조) | 다시 고르기 |

`다시 고르기`는 후보 선택(화면 09)으로 돌아간다 — **잘못 골랐을 수 있기 때문이다.**
`나중에`나 `취소`를 두지 않은 이유: 여기서 빠져나가면 등록이 안 된 채로 끝나는데,
그건 사용자가 원한 결과가 아니다.

### 화면 17 지역 랭킹의 빈·실패 상태 (2026-08-08 추가 · AOS 세션)

**왜 추가했나.** 랭킹을 서버에서 받게 하면서 화면 17이 **문구가 없는 상태 네 개**를
갖게 됐다. 2절 17번 표는 "랭킹이 있는 화면"만 적혀 있어서, 없을 때 화면이 텅 빈다.
**오너 검토 대상이다.**

**실측으로 확인한 사실이 이 표의 근거다.** 서버 `region_ranking`은
① 활동 지역을 안 정했을 때와 ② 정했지만 그 지역에 이번 시즌 발견이 0건일 때
**똑같이 빈 응답**을 준다. 두 상태를 화면이 구분해야 하는 이유는 **①은 사용자가
할 일이 있고 ②는 없다**는 것이다. ①에서 "아직 이웃이 적어요"라고 말하면
**할 일이 있는 사람에게 기다리라고 하는 것**이 된다.

| 상태 | 문구 | 버튼 |
|---|---|---|
| 불러오는 중 | (기존 로딩 표시만 · 문구 없음) | — |
| 지역 미설정 | 활동 지역을 정하면 순위를 볼 수 있어요 / 같은 동네 이웃들과 꽃 수집 순위를 겨루게 됩니다. | `동네 선택하기` (Primary) |
| 이웃 발견 0건 | 아직 이 동네에 모은 꽃이 없어요 / 첫 번째로 남겨보세요 | `꽃 찍어보기` (Primary) |
| 조회 실패 | 연결이 불안정해요. 잠시 후 다시 시도해 주세요. | `다시 시도` (Secondary) |

- `지역 미설정`의 설명은 **화면 02의 `설명 1`을 그대로 쓴다** — 같은 것을 설명하는
  두 화면이 다른 말을 하면 사용자가 다른 기능으로 읽는다.
- `이웃 발견 0건`은 3절 빈 상태의 `지도에 기록 없음`(`아직 이 근처에 공유된 꽃이
  없어요 / 첫 번째로 남겨보세요`)과 **같은 구조**로 맞췄다. 지도와 랭킹이 같은
  "아무도 아직 안 했다"를 다른 말투로 말할 이유가 없다.
- `조회 실패`는 3절 토스트의 네트워크 오류 문구를 재사용한다. **새 문장을 만들지 않았다.**
- ⚠️ **`아직 이웃이 적어요`(2절 02번)를 여기 쓰지 않았다.** 그건 이웃 10명 미만일 때
  화면 02의 목록 항목에 붙는 라벨이고, 화면 17에서 이웃이 적다는 것은 **B-6 규칙이
  구 단위로 넓혀서 이미 해결한 상태**다(그때는 랭킹이 정상적으로 나온다).
  같은 문구를 다른 뜻으로 쓰면 둘 중 하나가 반드시 틀린 말이 된다.
- 🔴 **범위가 구로 넓어졌을 때(`scope = gu`) 제목을 `{동명}`으로 두면 거짓말이 된다.**
  서버가 `scope`를 돌려주는 이유가 그것이다(0002 SQL 2절 주석). 이 경우의 문구는
  **오너·디자이너 확인이 필요하다** — 아래 4절 7번에 올렸다.

### 친구 수를 모를 때의 문구 (2026-08-08 추가 · AOS 세션)

**왜 추가했나.** 2절 18·19·20에 친구 수가 세 번 나오는데 **전부 숫자가 박힌
문장**이다(`친구 8명과 겨루는 중` · `내 친구 {n}` · `친구 관리 8명`).
친구 수는 서버에 물어야 알 수 있고, **못 물어보는 경우가 있다**(연결 실패 ·
불러오는 중 · 서버 기능이 꺼진 빌드). 그때 쓸 문장이 없어서 추가했다.
**오너 검토 대상이다.**

| 화면 | 아는 경우 (기존) | 모르는 경우 (추가) |
|---|---|---|
| 18 제목 | 친구 8명과 겨루는 중 | 친구와 겨루는 중 |
| 19 탭 | 내 친구 8 | 내 친구 |
| 20 메뉴 | 친구 관리 `8명` | 친구 관리 (값 칸을 비운다) |

- 🔴 **모를 때 `0명`을 쓰지 않는다.** `친구 0명과 겨루는 중`은 **틀린 말**이고,
  게다가 0명은 화면 18에서 초대 화면으로 전환되는 조건이라 **친구가 있는 사용자에게
  `초대 링크 보내기`를 내민다**. 이미 친구인 사람을 다시 초대하라는 화면이 된다.
- 🔴 **모를 때 랭킹에 뜬 친구 수로 대신하지 않는다.** 서버 친구 랭킹은 **이번 시즌
  발견이 0건인 친구를 아예 안 준다**(실측). 그 숫자를 쓰면 화면 18이 `친구 3명`,
  화면 20이 `친구 관리 8명`이 되어 **같은 것을 두 숫자로 말한다.** (18)에서
  실제로 7 vs 8이 났던 자리다.
- 문장 구조를 바꾸지 않고 **숫자만 빼는 방식**으로 골랐다. 새 문장을 만들면
  "친구 기능에 문제가 생겼다"로 읽히는데, 사실은 숫자 하나를 못 받은 것뿐이다.

### 화면 20·21에서 **값이 없는 칸** (2026-08-08 추가 · AOS 세션)

**왜 추가했나.** 2절 20·21번 표는 값이 **항상 있는 화면**만 적혀 있다
(`꽃보다효산` · `우리 동네 꽃박사` · `배지 3개` · `연남동 4위`). 더미를 지우고
서버·기기 기록을 붙이니 **칸마다 따로 비어 있을 수 있다.** 특히 화면 21은
시즌 종료 후 **본인이 안 눌러도 강제로 뜨는 전면 화면**이라(주석 ①) 값이 없을 때
무엇을 쓸지 정해 두지 않으면 그 화면이 거짓말을 한다. **오너 검토 대상이다.**

| 칸 | 아는 경우 (기존) | 값이 없을 때 |
|---|---|---|
| 20 프로필 닉네임 | 꽃보다효산 | 연결이 불안정해요. 잠시 후 다시 시도해 주세요. + `다시 시도` (Secondary) |
| 20 프로필 둘째 줄 | 연남동 · 2026년 3월부터 함께 | 있는 것만 쓴다 (`연남동` 또는 `2026년 3월부터 함께`), 둘 다 없으면 줄을 뺀다 |
| 20 대표 칭호 | `우리 동네 꽃박사` | (칩을 그리지 않는다) |
| 20 지표 3칸 | 모은 꽃 `37종` · 총 발견 `112회` · 공유 `26개` | (읽는 중에는 3칸을 그리지 않는다) |
| 20 내 배지 | 3개 · 전체 보기 + 카드 3장 | 아직 받은 배지가 없어요 / 시즌이 끝나면 받을 수 있어요 (3절 `배지 없음` 재사용) |
| 20 활동 지역 | 서울특별시 마포구 연남동 | 활동 지역을 정하면 순위를 볼 수 있어요 + `동네 선택하기` (화면 17 재사용) |
| 20 변경 가능일 | `변경 불가` · 2027. 2. 4. 부터 가능 | (줄 전체를 뺀다 — `변경 불가`만 남기지 않는다) |
| 21 결과·기록·칭호 | 연남동 4위 / 이웃 1,284명 중 … | 첫 시즌을 함께 했어요 / 다음 시즌에는 순위를 볼 수 있어요 |

- 🔴 **`변경 불가`만 쓰고 날짜를 빼면 안 된다.** 와이어프레임 20 주석 ④가
  "'변경 불가'만 쓰면 문의가 몰린다"고 이유까지 적었다. 그래서 날짜를 모를 때는
  **줄 전체를 뺀다** — 날짜 없는 `변경 불가`가 주석이 금지한 바로 그 화면이다.
- 🔴 **대표 칭호·배지에 예시 값을 남기지 않았다.** `우리 동네 꽃박사`는 1위 칭호이고
  `봄꽃 수집가`는 시즌 보상이다. 값이 없을 때 그리면 **받은 적 없는 사람에게 받았다고
  말하는 것**이고, 화면은 완벽하게 정상으로 보인다. 게임의 보상이 무의미해진다.
- 🔴 **화면 21의 `4위`를 기본값으로 두지 않았다.** 강제 노출 화면이라 사용자가
  아무 것도 안 해도 뜬다 — 한 번도 랭킹에 든 적 없는 사용자가 `연남동 4위`를 본다.
- `20 지표 3칸`을 **`0종`으로 그리지 않는 이유**는 화면 22(빈 상태)와 같다.
  읽는 중을 0으로 그리면 200종을 모은 사용자가 마이 탭을 열 때마다
  `모은 꽃 0종`을 한 프레임 본다.
- `21 값 없음` 문구만 **새로 썼다.** 다른 칸은 전부 기존 문장 재사용이다.
  이 두 줄은 3절 빈 상태의 어투(`아직 …없어요` / 대안 제시)를 그대로 따랐다.

### 화면 02에서 **이웃 수를 모를 때** (2026-08-08 추가 · AOS 세션)

**왜 추가했나.** 2절 02번 표의 목록 항목이 `서울특별시 마포구 연남동 /
이웃 1,284명 활동 중`이고, `이웃 10명 미만`이면 `아직 이웃이 적어요`로 바뀐다.
**두 문구가 같은 숫자 하나에 걸려 있는데, 그 숫자를 못 받는 경우가 있다** —
검색 결과가 한 화면에 여럿이라 동네마다 따로 물어야 하고, 그중 일부만 실패할 수 있다.
그때 쓸 문장이 없었다. **오너 검토 대상이다.**

| 상태 | 문구 |
|---|---|
| 이웃 10명 이상 (기존) | 이웃 1,284명 활동 중 |
| 이웃 10명 미만 (기존) | 아직 이웃이 적어요 |
| **모르는 경우 (추가)** | **(줄을 그리지 않는다 — 동네 이름만 남는다)** |

- 🔴 **모를 때 `0명`이나 `아직 이웃이 적어요`를 쓰지 않는다.** 두 문구 모두
  **"세어 봤다"는 뜻**이다. 못 세었을 때 쓰면 이웃이 1,284명인 동네를 **비어 있다고
  말하는 것**이고, 화면은 완벽하게 정상으로 보인다. 사용자는 그걸 보고 **6개월간
  못 바꾸는 선택**을 한다. 위 `친구 수를 모를 때`와 **같은 판단**이다 —
  숫자를 모를 때는 문장을 만들지 않고 **뺀다.**
- ⚠️ **줄을 빼도 동네는 고를 수 있다.** 이 줄은 참고 정보이고, 막아 세우면
  이웃 수 조회 실패가 **동네 선택 자체를 막는다.**

**🔴 이 숫자의 정의는 화면 17과 같아야 한다 — 서버에서 한 번만 센다.**

실측: `public_profiles`는 anon 키로도 `dong_code`별 개수를 준다(`206` ·
`Content-Range: 0-0/11`)므로 앱에서 바로 셀 수 있었다. **그런데 그건 가입자 수다.**
화면 17의 `{동명} 이웃 1,284명`은 서버 `region_ranking`의 `member_count`이고
**"이번 시즌 발견이 있는 사람" 수**다(0002 주석: "가입만 한 사람을 세면 10명을
넘겨도 랭킹이 텅 빈다"). 섞으면 화면 02에서 `이웃 12명`을 보고 고른 사용자가
화면 17에서 `이웃 3명`을 본다 — 위 친구 수 **7 vs 8과 똑같은 사고**다.
그래서 서버에 `dong_member_count`를 두고(0004) `region_ranking`과 **같은 집합**을 센다.
`아직 이웃이 적어요`의 기준 10명도 B-6의 최소 인원과 같은 값이다.

### 화면 03에서 **권한 카드가 두 장일 때의 제목** (2026-08-09 추가 · AOS 세션)

**왜 추가했나.** 2절 03번 제목이 `이 세 가지만 허용하면 준비 끝!`인데 **숫자가
문장에 박혀 있다.** 예선 빌드는 연락처 매칭을 넣지 않아 카드가 **두 장**이고,
그러면 제목이 **화면에 없는 세 번째 카드를 세고 있다.** 실측(에뮬레이터 온보딩 2/2)에서
카드는 2장인데 제목은 `세 가지`였다. **오너·디자이너 검토 대상이다.**

| 카드 수 | 제목 |
|---|---|
| 3장 (연락처 포함 · 기존) | 이 세 가지만 허용하면 준비 끝! |
| **2장 (예선 · 추가)** | **이 두 가지만 허용하면 준비 끝!** |

- 🔴 **숫자를 뺀 문장(`이것만 허용하면 준비 끝!`)으로 바꾸지 않았다.** `세 가지`는
  "몇 개만 하면 된다"를 **적게 느끼게 하는 장치**다(카드 3장이 한 화면에 다 보이는
  이유가 그것이다). 숫자를 빼면 몇 개인지 몰라 **끝이 안 보이는 절차**로 읽힌다.
- ⚠️ **이 줄은 위 `친구 수를 모를 때`와 판단이 다르다.** 거기서는 숫자를 **모르는
  것**이라 문장에서 뺐다. 여기서는 숫자를 **정확히 아는 것**(내가 그리는 카드 수)이라
  세어서 쓴다. 🔴 **모르는 값은 빼고, 아는 값은 센다** — 섞으면 둘 중 하나가 틀린다.
- ⚠️ 연락처를 되살리면 제목도 `세 가지`로 함께 돌아간다. 코드에서 **카드 수로
  고르게** 해 두었으므로(`PermissionIntroItem.all.size`) 따로 고칠 곳은 없다.

### 화면 14에서 **지도를 못 띄울 때** (2026-08-09 추가 · AOS 세션)

**왜 추가했나.** 2절 14번 표는 **지도가 나오는 화면**만 적혀 있다(검색창 · 필터 칩 ·
프리뷰 카드 · 길찾기). 그런데 카카오맵은 **타일 인증에 실패해도 예외를 던지지 않고**
회색 사각형만 남긴다. 그때 쓸 문장이 없어서 코드에 임시 문구(`지도를 불러올 수 없어요`)를
넣었다가 **A 문서에 없는 문구가 화면에 떠 있는 상태**가 됐다. 그래서 정식으로 올린다.
**오너·디자이너 검토 대상이다.**

| 상태 | 문구 | 버튼 |
|---|---|---|
| 불러오는 중 | (지도 배경만 · 문구 없음) | — |
| 내 기록 0건 | 아직 이 근처에 공유된 꽃이 없어요 / 첫 번째로 남겨보세요 (3절 `지도에 기록 없음` 재사용) | `꽃 찍어보기` (Primary) |
| **지도 인증·연결 실패** | 연결이 불안정해요. 잠시 후 다시 시도해 주세요. / 도감과 촬영은 그대로 쓸 수 있어요 | `다시 시도` (Secondary) |
| **지도 키가 없는 빌드** | (지도 탭을 그리지 않는다 — 문구도 버튼도 없다) | — |

- 🔴 **회색 지도를 그대로 두지 않는다.** 회색 사각형은 **"이 동네에 기록이 없다"로
  읽힌다.** 사용자는 꽃을 찍어도 지도에 안 나온다고 생각하고, 원인은 앱 밖(연결·콘솔
  설정)인데 **앱이 고장난 것으로 읽는다.** 위 `20 지표 3칸`을 `0종`으로 그리지 않는
  것과 같은 판단이다 — **값이 없는 것을 0으로 그리지 않는다.**
- 🔴 **`조회 실패`는 3절 토스트의 네트워크 오류 문구를 재사용한다.** 화면 17이 이미
  같은 문장을 쓴다(위 표). **새 문장을 만들지 않았다** — `지도를 불러올 수 없어요`는
  같은 상황을 다른 말로 말하는 것이고, 게다가 **"지도만 영구히 고장났다"로 읽힌다.**
  실제로는 잠깐 연결이 끊긴 것이라 **다시 누르면 된다.**
- 🔴 **`다시 시도` 버튼이 반드시 있어야 한다.** 실측(2026-08-09)에서 에뮬레이터 DNS가
  3초간 죽어 `MapAuthException(401)`이 났는데, 그 뒤 연결이 돌아와도 **화면이 오류
  상태에 갇혀 있었다** — 앱을 완전히 죽여야 지도가 다시 나왔다. 지하철·엘리베이터에서
  지도를 여는 것이 그 조건이다. 문구만 바꾸고 버튼을 안 주면 **말은 "잠시 후 다시"인데
  다시 할 방법이 없는 화면**이 된다.
- ⚠️ **두 번째 줄(`도감과 촬영은 그대로 쓸 수 있어요`)을 뺴지 않는다.** 지도가 안 되는
  것이 **앱 전체가 죽은 것으로 읽히면** 사용자가 촬영도 안 해 본다. 이 줄은 위
  `권한 재요청 시트`의 위치 문구(`도감 등록은 그대로 할 수 있어요`)와 **같은 구조**다.
- ⚠️ **키 없는 빌드에서는 문구를 띄우지 않는다.** 화면 17의 `NotConfigured`와 같은
  판단이다 — 눌러도 안 되고 **사용자 탓이 아니다.** 오류처럼 보이게 하면 안 된다.


### 코드에 있는데 2절 표에 없던 버튼 3개 (2026-08-09 추가 · AOS 세션)

**어떻게 찾았나.** 화면 04 헤더에서 개발용 `0종 보기` 버튼을 발견하고(진행.md (38)),
같은 종류가 더 있는지 **버튼 라벨 전량을 이 문서와 대조**했다. 27개 중 3개가 없었다.
전부 실제로 화면에 떠 있는 버튼이고, **문구를 새로 쓴 것**이다(규칙 위반).
지금 코드가 쓰는 값을 그대로 적는다 — 다른 문구가 맞다면 여기를 고치면 코드가 빨개진다.

| 위치 | 문구 | 왜 2절 표에 없었나 |
|---|---|---|
| 화면 02 헤더 (Ghost) | 나중에 하기 | 2절 02번 표에 **건너뛰기 수단이 없다.** 오너 결정이 "누구나 바로 플레이"라 건너뛸 수 있어야 하는데(지역은 랭킹에만 필요하다) 표가 그 경로를 안 적었다. ⚠️ 1절 Ghost 예시의 `나중에 할게요`와 **말이 다르다** — 같은 뜻의 버튼이 앱에 두 문장으로 있다. 통일 여부는 4절 11번 |
| 화면 12 3회 연속 실패 (Primary) | 도감 보기 | 2절 12번 표가 3회 실패를 **`도감 홈 복귀 유도`라고만** 적었다. 버튼 문구가 아니라 지시문이라 그대로 쓸 수 없었다 |
| 공통 헤더 (Text) | 뒤로 | 1절 44번 규칙(`아이콘만 있는 버튼은 만들지 않는다. 반드시 텍스트를 병기한다`)을 지킨 결과인데, **그 텍스트가 무엇인지는 어디에도 없었다.** 화면 05·15·16·19·21 헤더가 전부 이걸 쓴다 |

---

## 4. 디자이너 확인 요청 사항

| # | 항목 | 내용 |
|---|---|---|
| 1 | 서수 표기 길이 | 화면 11의 `이번이 열한 번째 발견입니다.`가 최장 문구. 한 줄에 들어가는지 확인 |
| 2 | 동명 길이 | `서울특별시 성동구 성수동1가`(13자)가 최장. 화면 02·20에서 잘리지 않아야 함 |
| 3 | 닉네임 길이 | 최대 10자 가정. 랭킹 리스트에서 종수와 겹치지 않는지 확인 |
| 4 | 칭호 길이 | `2026 시즌 1 연남동 꽃수집 1위`(19자)가 최장. 프로필 칩에 들어가는지 → 줄이려면 `연남동 1위`로 축약안 필요 |
| 5 | 다크 모드 | MVP 대응 여부 결정 필요. 미대응 시 시스템 다크에서도 라이트 고정 |
| 6 | 폰트 크기 확대 대응 | iOS 손쉬운 사용 '더 큰 텍스트' 사용률이 타깃에서 높다. 최소 130%까지 깨지지 않아야 함 |
| 7 | **화면 17 범위가 구로 넓어졌을 때의 제목** | B-6 규칙상 동네 이웃이 10명 미만이면 서버가 **구 단위 랭킹**을 준다(실측 확인). 그런데 2절 17번 제목·목록 라벨이 `{동명}`으로 고정이라 **`연남동 이웃 1,284명`이라고 써 놓고 실제로는 마포구 전체 순위**가 된다. 잠정으로 `{구명}`을 넣어 `마포구 이웃 N명`으로 두었으나, "우리 동네" 탭 이름과 어긋난다. 문구 확정 필요 (예: `마포구 이웃 N명 · 연남동 이웃이 모이면 동네 순위로 바뀌어요`) |
| 8 | **화면 20 대표 칭호 · 내 배지의 데이터 출처** | 주석 ①이 칭호 **상시 노출**을 요구하는데 서버에 칭호·배지 테이블이 없다(01 스키마). 지금은 칭호 칩을 안 그리고 배지는 `배지 없음` 빈 상태로 둔다. B-9(배지 종류) 확정과 함께 **어디서 받아오는지**가 정해져야 이 칸이 채워진다 |
| 9 | **화면 21 지난 시즌 결과의 데이터 출처** | 순위·최고 순위·수여 배지를 보관하는 테이블이 없고, 서버 `my_season_summary`는 **이번 시즌만** 센다(실측). 시즌이 끝나면 그 값이 사라지므로 **시즌 종료 시점에 스냅샷을 남기는 서버 작업**이 필요하다. 없으면 이 화면은 영구히 값 없는 상태다 |
| 10 | **화면 14 지도 실패의 시각 처리** | 3절에 문구는 정했으나(위 `화면 14에서 지도를 못 띄울 때`), **회색 지도 위에 무엇을 덮을지**가 안 정해졌다. 지금은 배경을 흰색으로 덮고 안내를 중앙에 둔다 — 지도가 잠깐 보였다가 사라지는 것으로 보인다. 대안: 반투명 오버레이로 지도를 남겨 두거나, 카드 형태로 하단에 띄우기. **`다시 시도`를 누를 수 있어야 한다는 것만 고정이다** |
| 11 | **`나중에 하기` vs `나중에 할게요`** | 같은 뜻의 회피 버튼이 앱에 **두 문장**으로 있다 — 화면 02 헤더는 `나중에 하기`, 화면 12 Ghost는 `나중에 할게요`(1절 Ghost 예시도 후자). 중장년 타깃이라 종결 통일이 스펙의 일부인데 여기가 갈려 있다. **하나로 정해 주면 코드를 맞춘다** — 지금은 코드 현행을 3절에 적어 두었다(위 표) |
